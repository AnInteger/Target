# Quickstart: 004 UI v2 重构

端到端验证指南。前置基线：`flutter analyze` 0 issue、`flutter test` 128/128 绿（003 完结态）；schema v4 库一份（真机/模拟器均可，桌面 web 亦可走查大部分项）。

## 阶段 A · 原型评审门禁（FR-015，实现前）

1. `design/prototypes/` 下逐屏打开延展界面走查稿（编辑器/详情/我的/通知/全部列表）
2. 逐屏裁决（通过/返工），轮次与结论记 `design/reviews.md`
3. **全部冻结后才允许进入阶段 B 之后的对应实现**（tasks.md 会以门禁任务排序）

## 阶段 B · 令牌与主题（US1）

```bash
flutter test test/token_contract_test.dart   # 三端令牌对账（tokens.css ↔ dart ↔ swift）
```

- [x] 系统深色：逐页走查（今日/回顾/我的/编辑器/详情/通知）全深色语言，无残留旧风格组件
- [x] 系统浅色：同上，浅色语言与深色同构
- [x] 我的页主题三档：切换即时生效、无需重启；重启后保留；跟随系统档随系统切换
- [x] 全部界面无口语化解释句、无本地存储说明

## 阶段 C · 健康度对账（US2，契约 health-score.md §验收对账）

1. 造数：健康类 2 目标（其一最后记录在 7 天前）、习惯类 1 目标（今日打卡）、目标类 1 目标（暂停中）
2. [x] 三环：健康 97 / 习惯 100 / 目标呈无数据态（暂停不参与）
3. [x] 给健康零记录目标打卡 → 健康环即时 100（无重启无切页）
4. [x] 恢复暂停目标（其 7 天零记录）→ 目标环出现且 97
5. [x] 全部目标暂停 → 环区隐藏、今日页空态接管

```bash
flutter test test/health_score_test.dart    # 口径纯函数全量对账（边界：0 分夹持/无数据/跨天窗口右移）
```

## 阶段 D · 导航与今日页（US2/US3/US4）

- [x] 任意页面底部恒为「今日 | 中央＋ | 回顾」；中央＋直达编辑器（≤1 次交互）
- [x] 今日页头部：中文星期日期 + 大标题「今日」+ 头像 → 我的页；主题/通知/备份职能 ≤2 次点击可达
- [x] 轮播：多目标可左右滑、最近互动居首；主行动按钮进记录动线；「查看全部」→ 全部列表（筛选/管理可达）
- [x] 暂停某轮播目标 → 即时移出；单目标退化单卡无滑动指示

## 阶段 E · 回顾页与初始屏（US5/US6）

- [x] 三区块依序呈现，周平均与手工核算一致；七天圆点打卡日着色、未来日不呈完成态
- [x] 周切换往周数据正确；上周无数据 → 环比「无可比较」
- [x] 清数据冷启动：初始屏（深底品牌图形+主张+开始使用）→ 今日页；再次启动不再出现

## 阶段 F · 全功能回归（FR-016 / SC-006，收口）

- [x] 创建：三类型各建一个（习惯开提醒选频率+时间）、分类图标选择、一句话描述保存
- [x] 打卡（含选填描述）/ 补签 / 撤销；到点提醒语气一致
- [x] 编辑 / 暂停 / 恢复 / 删除；短期到期提醒 + 标记达成 / 续期
- [x] 通知列表（倒序/空态）；资料编辑；备份导出→清库→恢复（含 themeMode 键往返）

```bash
flutter analyze && flutter test              # 0 issue + 全绿（含既有 128 项零回归）
```

## 完成口径

SC-001…SC-006 逐条核对（2026-08-23 收口，代码层全过；真机侧载轮沿用 003 T043 合并窗口）：

| SC | 核对结论 | 依据 |
|---|---|---|
| SC-001 深浅双走查 0 可读性失败 | ✓ | T032 双主题四页走查用例 + `tool/contrast_audit.dart` 全量矩阵（正文级 4.5 / 图形级 3.0）；四枚令牌加深后全对 PASS（浅 accent #2196F3→#1565C0 等），三项冻结设计固有项留档 reviews.md |
| SC-002 今日页骨架 100% 对应冻结稿 | ✓ | T032 走查逐元素映射：头部（日期/大标题/头像）、三环区、轮播卡、dock 三签＋中央＋，无骨架外元素 |
| SC-003 主题三档即时生效 100% | ✓ | T012 我的页主题组用例：切档即时重绘、重启保留（themeMode 键）、跟随系统档随平台亮暗切换 |
| SC-004 三统计与手工核算一致（含回升路径） | ✓ | T027 stats_engine 33 例纯函数口径；T018 health-score 全量对账（0 分夹持/无数据/跨天窗口）；T021/T029 部件级回升（健康 97→100 即时、目标环出现 97、全暂停空态接管） |
| SC-005 初始屏出现且仅一次、≤1 击 | ✓ | T030 SC-001 重写用例：清数据冷启动品牌屏→点「开始使用」1 击进今日页；onboardingCompleted 落库后不再出现 |
| SC-006 既有功能零丢失 | ✓ | T031 FR-016 15 项逐条映射（创建三类型/打卡补签撤销/提醒语气/编辑暂停恢复删除/到期续期/通知/资料/备份往返/统计），证据全部落 reviews.md 004 T031 条目 |

`design/reviews.md` 中本轮全部送审屏结论为「通过」（含 R3 返工裁定）；tasks.md 全勾。任务收口态：`flutter analyze` 0 issue + `flutter test` 161/161 全绿。
