# 原型评审记录（PrototypeReviewRecord）

> 流程实体载体（data-model.md §4 / 契约 prototype-review.md）。
> 规则：某屏最新结论为「通过」前，该屏不得进入 Flutter 实现（FR-001）；
> 记录字段：`屏/方向 · 轮次 · 日期 · 用户意见原话 · 结论 · 修订说明`。
> 含糊表述（"还行/先这样"）按「修改后再审」处理并追问具体意见。

## 风格方向决策（FR-009）

- 状态：**已定稿（2026-08-19）**——「柔彩仪表盘」（`direction-g.html` v3）经第 3 轮 R3-3 确认通过
- 参与对比的方向：第 1 轮 A 印记手账 / B 制图账本 / C 贴纸收集册；第 2 轮 D 系统 Today / E 活力数据 / F 玻璃层次；第 3 轮 G 柔彩仪表盘（参照图复刻，v1 皮肤级 → v2 结构级 → v3 细节对齐）
- 选定结论 / 融合说明：**G「柔彩仪表盘」全量采纳**。保留前序轮次的用户认可元素：年度时间带（源自 B 的倒计时概念、用户提议改为年尺度）作为首页白玻璃卡承载。核心语言：粉紫四段渐变底幕 + 玻璃壳两层景深 + 黑/青柠双强调（主操作黑实心、进度与完成青柠 `#b5e550`）+ Inter/PingFang 双栈 + 8pt 网格。
- 日期：第 1 轮 2026-08-18 · 第 2/3 轮 2026-08-19 · 定稿 2026-08-19

## 实现审计

### 004 T032 双主题全页走查与图表可辨核验（SC-001/002/003 + FR-013 · 四令牌对比度合规修复）· 通过 · 2026-08-23

- **走查方式**：①令牌级对比度矩阵计算（`tool/contrast_audit.dart` 可复现，WCAG AA 正文 4.5 / 图形 3.0，含玻璃卡 α 合成底）；②深色档全页冒烟（新用例「T032 双主题走查」——themeMode=dark 下今日/回顾/全部目标/详情四页身份渲染 + 逐页 `identical(palette, TargetPalette.dark)` 断言深色令牌全链安装）；③FR-013 逐元素代码核验；④SC-002 骨架对账；⑤SC-003 指 T012 既有用例。
- **问题项修复（四枚可读性令牌，三端同步 design_tokens.dart ↔ tokens.css(v=0823c) ↔ DesignTokens.swift，构造/色相族/角色不变）**：
  1. 浅 accent `#2196F3→#1565C0`（蓝 800 同族加深）——accent 作正文（「查看全部 ›」×2、详情恢复链接、里程碑添加 chip、今日列高亮标签）压白卡/灰底/渐变暗端 3.12/2.9/2.87 → 全配对 ≥5.06；白字按钮标签（accentOn×accent）3.12→5.75；
  2. 浅 onSurfaceVariant `#6E6E73→#6A6A70`——压渐变暗端（空态长文位）4.46→4.73，压灰 4.54→4.82；
  3. 浅 warning `#B26100→#9A5700`——↓环比胶囊压 surfaceAlt 4.09→5.04（003 审计「理论配对不触发」项在 004 生成实际组合，随屏补齐）；
  4. 深 accentOn `#FFFFFF→#00263B`——深色亮蓝实底白字按钮 2.43→6.46，与 positiveOn 深墨同构（亮填充配深墨字）。
- **复走查**：修复后矩阵正文级双主题 0 FAIL（19 配对/主题）；图形级仅 divider×surface 1.26/1.32 为装饰项（发丝线与环空轨，信息由弧长+中心数字+对勾/描边承载，WCAG 装饰元素豁免）。全量门禁 analyze 0 + test 161/161（新增 T032 用例）。
- **FR-013 双编码逐元素核验（全过）**：健康度环=弧长编码分数+中心数字+无数据段空置图例「—」；圆点行=full 实底对勾/partial 3px 描边圈/none 灰底（形状三态）；线性进度=长度编码；环比=↑/↓ 箭头字符。四类均不单靠色相。
- **SC-002 今日页骨架对账（基准图 01/02 逐区块 100%）**：头部（日期行+displayL「今日」+44px 头像→/settings，T020）｜进度环（案 C 单环三段弧+图例，T020）｜关注卡轮播（大类渐变卡+白胶囊记录打卡+页点，T021/T022）｜查看全部→/goals-all（T022/T029 动线用例）｜导航 dock（今日|中央＋|回顾 D2 黑线，T025）——每块带测试锚点，深色面由新 T032 用例补全。
- **SC-003 主题三档**：T012 用例（system/light/dark 逐档 `identical(palette, …)` 即时断言+落库持久+跟随系统回切）。
- **留档（冻结设计固有，不属本任务修复面，供后续设计轮裁定）**：①关注卡白字 × 大类渐变 a 端 2.12–3.12（habit 最弱）——R3 统一梯度 135°·L61→L45 为用户冻结构造，改深 a 端或加字影均重开设计稿，未擅动；②大类图标色 × 浅色 surfaceAlt tile 1.93–2.80（图形级）——类身份另有组头/筛选 chip 文本承载，图标为冗余编码（003 同类先例留档）；③divider 见上。原型缓存版本 ?v=0823b→0823c（九张 v2 稿换令牌后强制重载）。

### 004 T031 FR-016 全功能回归走查（quickstart 阶段 F 全动线 · 可执行证据映射）· 通过 · 2026-08-23

- **基线**：`flutter analyze` 0 issue；`flutter test` 160/160 全绿（T030 收口态——v2 七屏换装各屏动线用例全量重写后全量回归）。
- **走查方式**：阶段 F 逐项以 widget/服务测试为可执行证据映射（004 每屏换装任务均随屏重写动线用例，套件全绿即动线走查通过）；真机手感/帧率/通知实收面沿用 003 T043 用户侧载轮归属，不在本条范围。
- **清单映射（15 项全过）**：
  - 创建三类型：T023 创建动线（一句话→短期默认直存落库）/ T025 提醒组（习惯默认开→每日·3 天·每周三档）/ T023 骨架（改型联动显隐）+ SC-002 计步（描述+类型+提醒+图标 ≤8 击，页签全程在场）
  - 分类图标选择：T026 分类组（常用行 6 枚 + 「更多」弹层十域分组，单选即存 iconKey）
  - 一句话描述保存：T024（40 字上限+完整短句示范）；T023（落库无心理字段写入路径）
  - 打卡（含选填描述）+ 撤销：T021 详情（打卡描述落库）；US2 微缩验收（打卡→进度/成就即时刷新→撤销回退）
  - 补签：T014 弹层（详情周点阵点过去日→日历改选→isBackfill 落库+toast+历史「补签」标；004 入口自今日长按迁详情，随屏改写）
  - 到点提醒语气一致：reminder_service（daily 标题=目标名·正文轻推·留痕后静默；deadline 当日 09:00「到日子了」单次；关开关即时取消）
  - 编辑：T023 编辑同构（类型锁定 004 冻结稿+未动字段原值继承）；T025（编辑回填提醒行原样续写）
  - 暂停 / 恢复：goals_all T023（长按管理 sheet 暂停|恢复 canTransitTo 显隐全动线）；health（恢复 7 天零记录目标→环出现且 97）
  - 删除：T014（二次确认→打卡/步骤/提醒/频率版本四子表级联清行+退出详情）
  - 短期到期提醒+标记达成 / 续期：T029 到期询问（双入口+achievedAt 落库）；T029 超期（持续提示+仍可打卡+续期改 deadline）
  - 通知列表（倒序/空态）：notification_list（四源混排时间倒序·明日组居首 / sheet 空态 / 38px 语义色行形态+行尾相对时刻 / 条目 tap→详情页签不退场）
  - 资料编辑：profile（sheet 昵称输入+4 列头像格→落库往返 / 再点选中格回默认枚 / 昵称 12 字上限）
  - 备份导出→清库→恢复（含 themeMode 往返）：backup_test（全实体+设置逐字段往返；themeMode 双向宽容——可选项往返+缺键/未知值→system）+ V7 widget 动线（导出 toast→本地有数据必显式确认→覆盖→计数 toast）
  - 统计：stats_engine 周口径对账（周平均/环比/点阵三档/周完成度 33 例，含手工核算 5/6→83%）+ 回顾 v2 三区块 widget 走查（T021 周切换 / T029 点阵与目标卡 / T031 空周 CTA）
- **结论**：FR-016 能力面在 v2 信息架构（dock 两页签+中央 FAB / 详情周点阵补签 / 编辑器模板条 / 我的页迁头像 push）下全数保全、无能力回退；旧入口迁移均有测试锚点（引导模板 chip→编辑器模板条 T019 重写；今日长按补签→详情日历 T014 改写；/goals 页签→redirect 兜底）。004 首条实现审计，后续 T032 双主题走查 / T033 收口对账 / T034 文档收口逐条续录。

### T042 文档收口（quickstart SC 逐条 + spec Status + 全特性终检）· 通过 · 2026-08-22

- **SC-001…007 逐条核对**：落 quickstart「完成口径」表——七条全过，各带用例锚点（T022/T030/T038/T032/T033/T036/T045/T040）；SC-003 真机面与 T043 首启核对互补。
- **spec.md Status**：Draft → Complete 2026-08-22（全链收口摘要 + 两处裁决等价调整记录 + 真机轮待执行）。
- **终检**：本特性送审/验收结论全录本文件「实现审计」（T017–T045 连续条目，含 R3/R4 返工与 T038 真缺陷修复记录）；tasks.md T001–T043（无 T044）全勾；基线 `flutter analyze` 0 + `flutter test` 128 全绿。
- **memory 同步**：`target-implement-state.md` 更新至 003 收口态（004 新会话入口）。

### T043 真机合并轮清单（用户侧动作整理 · quickstart 阶段 C-3）· 清单就绪 · 2026-08-22

- **动作链七步**（quickstart.md 阶段 C-3）：Codemagic 手动 `ios-unsigned` → iLoader 自签覆盖装机（不清数据）→ 装后首启核对 → 视觉 → 手感 → 通知 → 结论回写本文件。
- **真机存量库 = v2→v4 迁移真实样本**：覆盖装机不清数据，旧目标全数在今日页可打卡、详情提醒行保留原时间、旧字段零上屏（SC-003 真机面；代码层已由 T038 对账与 SC-006 渗漏用例覆盖）。
- **核对点映射**：新图标/启动屏（T028「今日之环」A 案首次真机亮相）｜三 Tab 手感（FR-001）｜打卡动效帧率（FR-019 肉眼顺滑）｜通知语气（FR-012/021 真机独占：到点提醒正式语域、短期到期「到日子了，怎么样？」、点通知落地详情）｜深色模式。
- **执行归属**：用户「顺带测试」合并轮（002 侧载回路沿用：免费 Apple ID，无 TestFlight）；agent 侧代码层验证已全数收口，真机结论由用户回填。

### T045 文案语域清查（FR-021 · 违禁处置 + 死键清理 + 全屏走查）· 通过 · 2026-08-22

- **基线**：`flutter analyze` 0 issue；`flutter test` 128 全绿（清查前后两次全量）。
- **违禁常量处置**：`onboardingDataNote`（引导页「数据只存在这台设备上，不上传云端」）与 `privacyFoot`（设置页「数据只在这台设备上……记得定期导出备份。」）删除——常量 + 消费方（onboarding.dart 说明行、settings_view `_PrivacyFoot` 卡与 `_DashedRRectPainter` 专用 painter、widget_test 断言）整链移除；FR-021「本地存储位置说明不得出现在任何界面」闭环，测试补渗漏断言 `textContaining('这台设备') findsNothing`。
- **口语化正式化**：`reminderNudge`「今天还没记录，做一次就算数。」→「今天还没有记录。」；`milestoneStepHint`「一步就好，别贪多」→「一句话描述这一步」（走查补获，空态说明与输入 hint 双用）。
- **死键清理 55 个**（002 遗留为主：editor B 案 envelope 组 / goals 管理页组 / 生活电量 / 连击 / cue 场景档 / 通知多目标标题等）——`Copy` 无引用常量全删，三个成员清空的分组注释一并移除。注：行内尾注释常量的删除曾越界吞并相邻 `reviewWeekSum`（活键），analyze 兜底发现后已原位恢复——行尾注释不以 `;` 收束是删除脚本的盲区。
- **保留项裁定**（R3/R4 评审定稿的教练式语域 = ui-contract「文案与语气」基线，非 FR-021 所禁「口语化解释句」）：reviewCoach* 三档观察语、`reviewEmptySub`、`focusLimitTitle/Body`、`goalPauseHint`、`dailyBrief*`「不着急/安心过今天」、`backfillHint`、`onboardingSkip`「先随便看看」、`milestoneOverdue`「不急，下一步是什么？」；用户裁定通知文案 `notifDueTitle`「到日子了，怎么样？」原样保留。
- **全屏走查**：今日/回顾/我的/详情/编辑器现存文案逐条过审；UI 层裸字符串抽查仅数据类（模板内容 / 周标签 / 对话框动作 / 导入计数实体名），无寒暄、比喻式提示、本地存储说明残留。

### T040 V1–V8 全回归（001 口径在新信息架构下逐条映射 · FR-017/SC-007）· 通过 · 2026-08-22

- **基线**：`flutter analyze` 0 issue；`flutter test` 128 全绿。
- **V1 创建 ≤30s ✓**：空库首启 → 引导 → 模板 → 编辑器预填 → 落库 → 今日可见（'T019 V1 创建双路径' 用例）；003 入口等价调整 = 编辑器三型分段 + 图标库单选，步数不增（T030 SC-002 计数用例 ≤8 交互含全部字段）。
- **V2 打卡闭环 ≤10s ✓**：'US2 微缩验收'（打卡 → 进度/成就即时刷新 → 撤销回退）+ 'T010 成就时刻'（上升沿/退场/重臂）；**打卡主路径 = 今日页单 tap，1 交互 ≤2 ✓**；全完成重进成就态非空白。
- **V3 小组件 → 真机项**：iOS 专属（T043 清单复核）；降级深链兜底 = `target://goal/{id}`（US1 用例断言，映射不回退无响应）。
- **V4 周结算/周回顾 ✓（等价调整）**：'T021 周回顾 R3'——周摘要/图例/节奏条/观察语全呈现；settlement_test R1–R9 口径断言全绿；003 裁决「调整频率决策控件退役」（频率体系删除的必然收口，spec 明定非回退），完成率/趋势/补签次数能力保留。
- **V5 补签 ✓**：'V5 长按 → 补签日历 → 补昨日 → 带补标记' 用例；迁移补签保全（T038 逐项对账）。
- **V6 忙碌模式 ✓（spec 裁决：随频率体系退役）**：降档对象（每周 N 次频率）已删除，一键降档无语义对象；等价保全 = 忙碌记录数据零丢失（备份往返含 busySessions + 迁移不动该表）+ 我的页「忙碌记录」行只读呈现。
- **V7 备份闭环 ✓**：'T026 + V7 备份回归'（新设置入口下导出 toast/冲突弹层/覆盖计数）+ T037 v1..v4 双向宽容互导。
- **V8 通知降级 ✓**：reminder_service_test「权限被拒 → 全量取消、零调度（FR-007）」+ 设置页权限说明行在场（T034/T036 渲染断言）。

### T041 深链与外部入口回归（target:// 全映射 + 通知落地页 + 小组件路径）· 通过 · 2026-08-22

- **基线**：`flutter analyze` 0 issue；`flutter test` 128 全绿（127→128，含 T041 通知落地用例）。
- **深链全映射 ✓**：US1 路由用例扩展断言——`target://today`→/today、`target://review`→/review、`target://goal/{id}`→/goal/{id}、`target://goal` 无 id→/today 兜底；存量 `/goals` 入口 redirect /today（既有断言）。
- **通知 tap 落地页 ✓（新用例）**：今日头部铃铛 → sheet 行提醒条目（今日/明日两条）tap → `/goal/{id}` 详情（打卡动线在场）且底部三页签不退场（today 分支子页）；遮罩下今日卡同名条目以 sheet 内查找规避误点（测试注记）。
- **小组件 tap 入口 ✓（代码层）**：widgetURL 与通知同走 `mapDeepLink`（上列断言覆盖）；真机实际行为归 T043 清单复核项。

### T039 US5 验收核对（spec 四条 acceptance 逐条 · 数据级 + UI 双层）· 通过 · 2026-08-22

- **基线**：`flutter analyze` 0 issue；`flutter test` 127 全绿（125→127，含 T039 两用例）。
- **场景 1（存量 100% 保留，计数逐项一致）✓**：T038 端到端用例——v2 四分支升级启动后目标/打卡/记录/补签走仓库读路径逐项对账（计数/状态/补签/归属原样，SC-003）。
- **场景 2（每日 3 次 → 习惯且计数连续）✓**：targetPerDay=3 频率档经 D3 映射 habit；迁移前连续留痕 6 天 → streakOf=6 不重置，迁移后继续打卡 → 7 无断层（migration_test T039）；UI 层今日卡直接呈现「习惯」徽章（节律点 + 文案，widget T039）。
- **场景 3（带截止 → 短期，倒计时正确）✓**：milestone+deadline → shortTerm；倒计时 = deadline − today 数据级断言 40 天（8/22→10/1），UI 层今日卡徽章「短期 · 还剩 12 天」与徽章同源算式一致（deadline 动态设 today+12 规避测试日期漂移）。
- **场景 4（退役字段零残留）✓**：T038 全界面正则终查（频率版本/颜色/为什么/怎样算/场景/旧图标键名，三 Tab + 详情含折叠线下卡面）；另修得真缺陷一例（详情提醒行 cueScene 回潮，见 T038 条目）。
- **注记**：frequency_versions.effective_from_week 类型化为 WeekStart（必须周一）——测试种子须 `WeekStart.containing(today)`，直写当天日期会 ArgumentError 挂构建。

### T038 迁移端到端对账（v2 存量升级 · 仓库对账 + 原节奏续排 + 旧字段零上屏终查 · SC-003/SC-006）· 通过 · 2026-08-22

- **基线**：`flutter analyze` 0 issue；`flutter test` 125 全绿（123→125，含 T038 两用例）。
- **仓库对账 ✓（SC-003）**：v2 文件库四分支（milestone+截止 / habit+daily+提醒 / habit+weekly 无提醒 / paused 无截止）→ 同文件 v4 升级启动 → 全走仓库读路径：goalType 按 D3 落位（shortTerm/habit/habit/longTerm）、deadline/envelope（为什么/怎样算）逐值保全、打卡计数/状态/补签/归属逐项一致、colorKey 退役置空、paused 状态机不动。
- **原节奏续排 ✓**：gd daily 存量提醒行原样续用（08:30 enabled，通知正文附存量「为什么」——FR-016 保全语义，「怎样算」不进通知）；gw weekly 补默认行（09:00 关）后打开，planReminders 周四不排/周六同 weekday 命中——档位语义经真实迁移行验证。
- **零上屏终查 ✓（SC-006）**：v2 存量文件库升级启动全 App 走查——今日（含折叠线下短期卡滚动后）/回顾/我的（滚动到底）/目标详情，旧字段（频率版本/颜色/为什么/怎样算/场景/旧图标域键名）零上屏正则扫查通过。
- **终查发现并已修（真缺陷）**：goal_detail 头部提醒行原以 cueScene 场景档上屏（「提醒 · 晚饭后」）——002 场景档体系已退役且不参与调度，展示口径失真；修法 = 提醒行真源改 Reminders 行（档+时刻，「提醒 · 一天一次 · 08:30」），cueScene 列留存库中但不再上屏（T021 用例同步改真源断言）。
- **测试基建注记**：testWidgets FakeAsync 区内真实文件 IO 永不完成——文件库搭建须包 `tester.runAsync`（本次踩坑定位：表象为 pumpWidget 前挂死无输出）。

### T036 US4 验收走查（设置行形态全标准 + 深浅两态 + V7 备份新入口 · widget 验收测试）· 通过 · 2026-08-22

- **基线**：`flutter analyze` 0 issue；`flutter test` 121 全绿（120→121，含 T036 用例）。
- **行形态全标准 ✓（FR-009）**：设置页 10 处 `_SettingsRow` 走查——图标格 32 + 标题/可空副题 + 行尾（值 `2`/时间 `08:00`/开关/箭头）全标准；非行形态仅契约明定的账号卡、权限卡（FR-007）、隐私脚注三处；二级嵌套行（按目标提醒展开）复用同一行形态（surfaceAlt 浅底 + 缩进，原型 s-nest 同构）。
- **深浅两态 ✓**：darkTheme 经 ThemeExtension 注入——T036 用例断言 `TargetPalette.of(ctx)` 与 `TargetPalette.dark` 同实例，深色下通知三行/二级展开/目标/数据/关于全行渲染无异常。
- **V7 备份回归 ✓（新入口）**：T026 用例走数据组 `_BackupCard` 行——导出 toast + 冲突弹层「覆盖本地」+ 计数 toast 全链在新入口下通过（T034 重构未动 `_BackupCard` 内核）。
- **走查发现并已修**：T035 总开关聚合缺口——简报无行时排程器视为「默认开」，关掉逐目标行后聚合视图被拉回 true；修法 = 总开关无论开/关都显式落一条简报行承载状态（T035 用例断言关后简报行必在）。

### T033 US3 验收走查（SC-004 空态直达 + 三屏标题对齐 · widget 验收测试）· 通过 · 2026-08-22

- **基线**：`flutter analyze` 0 issue；`flutter test` 118 全绿（116→118，含 T031/T032/T033 三用例）。
- **空态直达 ✓（≤1 交互）**：空库进 /review——空态竖直居中（量化断言：空态块中心 vs「标题底—导航顶」区域中心差 <20px、水平 <4px），tap「新建目标」CTA 一次直达编辑器且页签不退场（today 分支）。空态形态 = 原型画板②定稿：七格空圈节奏条（末格虚线 accent「将开始的那天」）+ 引导文案 + accent 胶囊 CTA。
- **三屏标题对齐 ✓（几何级，优于截图比对）**：新增屏级布局令牌（--screen-* ↔ AppScreen：padX 24 / titleTop 8 / titleBand 44，原型 st-top·gl-top 定稿规格）；断言回顾/我的屏题左缘与顶部**逐像素相等**，今日屏头部带与标题带竖直同带相容（差 <16px）——测试环境几何断言比并排截图更精确，真机截图归 T043 真机轮复核。
- **空数据三屏无死角 ✓**：空库下今日（空态邀请卡）/回顾（空态 CTA）/我的（账号卡+备份组）全渲染正常，三页签恒定。
- **附带修正**：设置页屏题原左缘 28/顶 16（双重 padding 叠加）→ 统一 24/8；今日页 ListView 顶垫 24→0（头部带自带 titleTop，向原型 st-scroll 对齐，头部带上移 24px）。
- 结论：**通过——US3 验收面（FR-007/008 · SC-004）收口**。

### T030 US2 验收走查（SC-002 计步 / SC-005 句式清零 · widget 验收测试）· 通过 · 2026-08-22

- **基线**：`flutter analyze` 0 issue；`flutter test` 115 全绿（113→115，新增 T030 两用例）。
- **SC-002 计步 ✓（实测 5 次交互，上限 8）**：完整 App（TargetApp + 三 Tab shell）从今日页走创建习惯动线——① ＋ 进编辑器 → ② 类型段「习惯」 → ③ 一句话描述 → ④ 常用行点图标 → ⑤ 保存；落库 habit/名称/iconKey，Reminders 行 isEnabled=true。「提醒」四要素之一零交互就位：切习惯默认开 + 频率默认一天一次 + 时间默认 09:00（均有断言）；即便用户逐项改（频率档 +1、时间选择器 +2）也只到 8 次打平上限。**页签全程在场**：动线每步后断言三页签（今日/回顾/我的）可见——编辑器为 today 分支子路由（FR-010），非全屏 modal。时间维度（≤60 秒）：5 次交互 × 单步即时响应 + 无页间跳转，测试驱动每步 pumpAndSettle 即时完成；人工口径由交互数与步骤数论证（无长表单/无向导分页）。
- **SC-005 句式清零 ✓（编辑器 + 设置区）**：编辑器侧 T023 用例已断言 why/criterion/cue 问句与颜色步 0 个（FR-014）；本走查补设置区——002 场景档词汇（早起后/午休时/晚饭后/睡前）与「多久做一次？」问答体在设置页断言 findsNothing，新口径短句 reminderGoalHint 在场。
- **走查发现并即修**：`settings_view` 的 reminderGoalHint 仍是 002 场景档句式（「……早起后/午休时/晚饭后/睡前；没选的 20:00 轻提醒……」）——内容与 003 排程契约（Reminders 行真源 + cadence 三档，T028）直接矛盾，属过时说明而非语域问题；改为「目标提醒在编辑目标时设置，按所选频率定时提醒。」
- 结论：**通过——US2 验收面（SC-002/SC-005）收口**；Phase 4 创建动线全链（T023–T030）完结。

### T022 US1 验收走查（FR-001~006 · web 构建 + Playwright 语义树）· 通过 · 2026-08-22

- **基线**：`flutter build web --release` 全新构建，`python3 http.server` 8391 本地服务；Playwright 开启 Flutter semantics 后按语义树驱动（真实点击）。analyze 0 issue；flutter test 104 全绿。
- **动线（新用户全链）**：引导页选「规律运动」模板 → 编辑器（002 B 案表单，US2 T023–T030 重建中，属预期中间态）→ 填「为什么」→ 保存 → 落 `#/today`。
- **FR-001 三 Tab 恒定 ✓**：今日/回顾/我的 三页签遍历正常（`#/today → #/review → #/settings`）；推入页（详情 `#/goal/<id>`、编辑器）底部导航同样保留恒三页签（各屏语义树逐屏佐证）。
- **FR-002 目标管理 ✓**：今日卡 tap → 详情（AppBar：编辑目标 → 编辑器；⋯ 更多操作 → 面板「暂停（先放一放，想回来随时继续）/ 归档（已归档，历史记录都在）」；习惯目标无「达成」项，与 canTransitTo 状态机一致）；**暂停 → 恢复往返实测**：暂停后状态行「暂停中 · 记录保留」出现、打卡动线隐藏；⋯ 面板切换为「恢复」；恢复后状态行消失、打卡动线回归。
- **FR-003 今日页无分隔线 ✓**（代码级 + 语义树侧证）：transparent Scaffold + ListView，无 AppBar（语义树今日页无 heading，头部带直接是账号区 group）、无 divider widget。
- **FR-004 账号编辑往返 ✓**：头部带账号区 → 资料 sheet（标题「编辑资料」/昵称默认「我」/8 枚头像）→ 改昵称「星行」+ 选头像「晴空」→ 完成 → sheet 关闭、头部带即时刷新为「星行 8月22日 周六」；「我的」页账号行同步显示。
- **FR-005 铃铛列表 ✓**：铃铛（角标 2 = 今日条目数）→ 「通知」sheet 按天分组（今天/明天），条目 = 目标提醒（规律运动 20:00）+ 每日简报（今天的小事 08:00）各带时刻；tap 条目 → sheet 关闭跳目标详情；**sheet 内无任何设置控件**（FR-006 口径：通知不带设置）。四类混排（提醒/到期/里程碑/成就）由 T019 单测覆盖（混排倒序用例），本次新用户动线只遇提醒+简报两类，属数据正常。
- **FR-006 设置集中 ✓**：今日页头部带仅账号区/铃铛/＋，无设置入口；设置整组在「我的」。
- **Findings（非阻塞，均有归属）**：① 引导页仍有 onboardingDataNote「数据只存在这台设备上，不上传云端」——R3 裁决「本地存储说明任何界面不出现」违例 → **T045 语域清查清掉**；② 设置页 privacyFoot「数据只在这台设备上——不上传、不联网……」同违例 → T045；③ 次要 a11y：账号区选中图形头像后按钮失去语义名（此前文字 fallback「我」提供标签），低严重度留档；④ widgetIosOnly「桌面小组件与提醒推送为 iPhone 专属功能」在 web 构建亦显示（002 遗留文案，随 T045 一并过语域）。
- 结论：**通过——US1 验收面（FR-001~006）收口**。

### T033 发布验证 · 用户侧完成归档 · 2026-08-22

- **用户确认原话（2026-08-22）**：「2.侧载成功，可以归档」——Codemagic `ios-unsigned` 手动构建绿、unsigned.ipa 经 iLoader 自签侧载真机运行正常；**T029 小组件首次 Swift 编译随之实证**（本机无 Xcode 的挂起点就此解除：DesignTokens.swift pbxproj 四条目 + BackgroundIntent 双 target 回路构建通过）。
- 真机核对口径：新 UI 整体运行正常为用户当前结论；**图标/启动屏当时仍为默认**（T028 尚未过链）——A 案资产已随后进链（见下方品牌图标条），真机看到新图标/启动屏需再手动触发一次构建 + 侧载。
- 归档：T033 [x]，002 全任务收口。

### T032 全量走查（quickstart 阶段 B + V1–V8 回归 + SC 逐条）· 通过 · 2026-08-21

- **基线**：`flutter analyze` 0 issue；`flutter test` 69 全绿（含 token 契约 SC-004、迁移零丢失、V5 补签、V7 备份、T021 纯回看、T026 设置+备份回归、FR-001 SMART、T017/T019 创建动线、US2 打卡闭环、T010 成就时刻、调度单测）。
- **web 实测（最终构建）**：今日页初始路由，1 次点击打卡圆钮 → 「好好吃饭 今日 0→1 次 / 最近 · 今天」即时刷新，刷新页面后保留（IndexedDB）——SC-002 ≤2 交互按 **1 次交互** 实测通过；驱动方式 = semantics DOM 开启后按钮几何定位 + 真实指针点击（按钮 aria-label 在合并树中丢失，为后续 a11y 改进留档）。
- **V1–V8 回归口径**：V1/V2/V5/V7/V8 测试钉住 + V2 另做 web 实测；**V4 决策动线（调整频率→下周一生效）与 V6 忙碌模式已由用户 2026-08-21 裁决废止**——按新口径核对：周回顾纯回看无决策控件（测试断言）、忙碌 UI 入口全清（lib/ 仅剩升级遗留会话收尾逻辑与备份计数标签）；V3 小组件挂 T033 TestFlight 真机，web 侧「iPhone 专属功能说明」占位存在（T030 截图核验）不报错。
- **SC 逐条**：SC-001 六屏全通过（今日 R4 / 目标列表 R2 / 编辑器 R1 方案 B / 周回顾 R3 / 设置 R2 / 打卡反馈随今日屏交互链）+ 方向决策留档（FR-009 2026-08-19）；品牌图标提案未过但非核心屏（T028 挂起）。SC-002 ✓（实测 1 次）。SC-003 ✓（69 测试 + 走查全过）。SC-004 ✓（token 白名单空，契约测试复跑绿）。SC-005 ✓（T030 审计）。SC-006 ✓（四主屏+编辑器+详情+onboarding 全部新语言，token 契约拦截回潮，**并存 0 屏**）。
- **渐进补全与零丢失**：T017 列表「为什么第二行 + 渐进补全邀请」测试绿；migration_test v1→v2 既有目标/check-ins 完整保留。
- 结论：**通过——US1–US5 验收面收口，进入 T033 发布验证**。T033 agent 侧已完成（analyze/test 全绿 + 45 提交 push 4666294..24277b5）；**Codemagic 为手动触发**（codemagic.yaml `ios-unsigned`：无 TestFlight 链路，免费账号回路 = unsigned.ipa → iLoader 自签装机），T029 小组件的首次 Swift 编译验证待用户触发构建。

### T030 深色模式全屏核对（对比度审计 + 8 屏视觉抽查）· 通过 · 2026-08-21

- **任务口径**：浅/深遍历全部屏，正文对比度 ≥4.5:1（SC-005），问题项修正并记录抽查结果。
- 结论：**通过——源码零修改**。正文配对矩阵（onSurface / onSurfaceVariant × surface / surfaceAlt / bgGrad 四档底幕，含 0.8 白 / 0.09 白玻璃卡混合底，浅深两套）全部 ≥4.5:1，深色侧全绿；无修正项。
- 边缘项（低于对应阈值但**非正文配对，不触发修正**，留档备查）：
  1. 浅色 goal-amber（#C98A1B）压白卡 2.94:1 < 3:1（图形级）——amber 仅作图形/强调色使用（忙碌徽记已随 T022 移除），无正文场景；
  2. 白图标压目标色底 ~2.9–4.0:1——目标色 18% 底上的同色首字为装饰徽记；实心按钮上的白图标（打卡钮等）有实底兜底且非信息文字；
  3. positive（#5C7D10）/ warning（#9A6700）压 surfaceAlt 4.26 / 4.34:1——理论配对，实际屏幕无此组合（两者只压白卡或作填充色出现）。
- **8 屏视觉抽查（最终 web 构建，全新字节集）**：无头 Playwright，视口 400×840，逐屏 `emulateMedia(colorScheme)` + 整页重载（about:blank → `#/path` → 4500ms）取屏；8 张全部经视觉模型核验（页面身份 / 选中页签 / 低对比 / 渲染异常）：
  - 浅色 4 屏全过：今日（「今天做了什么？」+ 3 目标）、目标（「3 个目标 · 本周留下 3 次记录」）、回顾（空态「上周还没有记录」）、我的；
  - 深色 4 屏全过：今日（0/3 环 + 今日行动/连续天数/进行中目标）、目标、回顾（空态同浅色）、我的（星行头像 / 提醒 08:00 / 备份与数据 / 隐私脚注逐区可读）；
  - **鉴别记录**：浅色「我的」屏首轮被视觉模型读成今日页——转写式复核（逐字转写大标题 + 逐项确认『星行』『提醒』『备份与数据』有、『今天做了什么？』无）确认实为「我的」页；且同构建同 URL 深色 settings 正常、go_router 不感知配色方案，浅色专属走错路由不成立 → 判定**视觉模型误读**，截图本身无误（md5 亦异于今日页截图）。
- **过程坑留档（后续走查复现需知）**：
  1. **陈旧投递假 404**——本地 python http.server 不发 Cache-Control，`main.dart.js` URL 无版本号被浏览器启发式缓存；干净源码冷加载 `#/today` 曾确定性 404（截图 md5 552521b4… = go_router 错误页，跨多次加载恒定）。探针证据（product 构建经 redirect 钩子打出 defaultRouteName=/today、location=/today，零 service worker）排除路由代码嫌疑；同源码全新 `flutter build web` + 页 URL 换查询串后消失。早前深色三屏 PASS 截图同属陈旧构建 → 已在最终构建全部重验。
  2. **hash 直航偶发不生效** → 逐屏整页重载纪律（about:blank 起跳），不采信同页 hash 切换后的截图。
  3. **屏幕身份对账用 md5**：Flutter web 渲染进 canvas、DOM 探针不可用，截图字节哈希 + 视觉模型双重锚定；截图上传 CDN 为内容寻址（同字节同 URL），哈希对账不因重传漂移。

## 屏幕评审

### 004 v2 原型 · 五延展屏 · 全数冻结 · 2026-08-23

- **送审物**：v2-goal-editor / v2-goal-detail / v2-settings / v2-notifications(?r3c) / v2-goals-all 五屏（R3 裁决已回填后的终态）
- **用户裁定原话**：「过」——五屏一次性全数通过
- **T011 门禁关闭**：七屏（含今日/回顾）全部冻结，冻结基准 = 上述七稿 + tokens.css?v=0823b 三端令牌（9656dfd）；T012–T016 与 T023 解锁，进入实现相

### 004 v2 原型 · v2-today.html · R3 落定（环定案·色系统一·dock D2 定稿）· 2026-08-23

- **R3 送审物**：v2-today.html R3（单环三段弧 + 统一梯度 + dock 去色两案对比）
- **用户意见原话**：「1.单环·三段弧 2.感觉都不好看，还是要原来的：http://127.0.0.1:8322/prototypes/v2-today.html?r2 这个吧，把颜色统一一下试试；导航条就不要颜色了，换个阴影或者黑色线条试试」→ 追问 D1（阴影胶囊）/D2（黑色线条）二选一，回：「D2」
- **裁决落定**：① **健康度环 = 案 C「单环·三段弧」定稿**（v2-alt-rings 六案对比产物）——一环三色各占 1/3 槽位、弧长=该类分数、中心=平均分；已回填 v2-today 四板与 v2-notifications 底幕两处。② **色系 = 四案全否，保留 R1 大类渐变卡语言**，但统一为同构梯度（135°·起点 L≈61/终点 L≈45·三色仅色相不同），卡内 CTA 胶囊文字改中性深灰、轮播页点去彩。③ **dock 去色 = D2「黑色线条」定稿**（D1 阴影胶囊落选弃用）——当前页签黑字加粗 + 16×3px 短横线（`::after`，深色模式经 `var(--on-surface)` 自动白线），FAB 中性化（on-surface 底 / surface 加号 / 4px 玻璃描边）。
- **回填完成（本轮）**：D2 dock 落到全部三个带 dock 的屏（v2-today 四板 / v2-notifications 底幕 / v2-review 三板）；梯度对收敛 tokens 三端——`tokens.css` 新增 `--grad-{health|habit|goal}-{a|b}` 浅深 6+6 键、`design_tokens.dart` 新增 `MajorGradient`/`MajorGradients`（Widget 侧无此面，Swift 不加）、`token_contract_test.dart` 契约表 +6 键对账；全部原型 `tokens.css?v=0823a` → `?v=0823b` 规避浏览器缓存旧变量；Playwright 结构化校验三屏（渐变浅/深解析值、页签色/线宽、FAB 反色）全过，门禁 analyze 0 + 133 绿。
- **T011 门禁状态**：R3 三项裁决全部落定并回填，但门禁仍**未放行**——editor/detail/settings/notifications/goals-all 五屏尚未经用户逐屏裁定冻结（FR-015：全部延展屏冻结前 T012+ 不得动工）。

### 004 v2 原型 · v2-alt-rings / v2-alt-colors 对比稿 · R2 修改后再审 · 2026-08-23

- **R2 送审物**：v2-alt-rings.html（健康度呈现六案 A–F）+ v2-alt-colors.html（全站色系四系 1–4，控制变量：同单环形态+统一紧凑卡 296px）
- **用户意见原话**：「1.抱歉，这个打卡圆环，现在看起来非常奇怪，你能多几种方案让我看看吗？比如单个圆环平均分这样的，你多找几个，不用参考现在的样式 2.目前看，首页卡片的大小和色彩选择看起来不是很舒服，包括其他页面的色彩一致性，感觉没有统一色系，有些颜色在一起感觉也很奇怪，没有美感（你找几个skill帮助设计一下）」
- **结论**：对比稿产出即裁决输入（spike 口径，落选方案弃）→ 用户于 R3 回复中点名案 C 并否决四色系（见上条）。
- **修订说明**：六案=单环平均分/三小环并排/单环三段弧/半环仪表盘/进度条卡/现行嵌套参照，统一数据 97/100/94；四系=静白蓝点睛/雾面浅彩/单色蓝/现行饱和彩参照。

### 004 v2 原型 · 全七屏 + R1 增补 · R1 修改后再审 · 2026-08-23

- **R1 送审物**：v2-{editor,goal-detail,settings,notifications,goals-all}.html（T006–T010）+ R1 增补 v2-today.html / v2-review.html
- **用户意见原话**：「1.还有肉眼可见的BUG，好像是有些地方没有图标 2.是不是还少了首页？」
- **结论**：修改后再审——① 定位为 gi() 返回 DOM 节点被 innerHTML 字符串化（"[object SVGSVGElement]"），六页注入改 replaceChildren 并以结构化 DOM 校验（data-gi 必含 svg）收口；② 今日/回顾两屏补入门禁（用户裁定）。
- **修订说明**：浏览器缓存坑以 ?r2 参数规避；图像分析模型曾误报图标在场，此后原型校验一律走结构断言。

### 003 四屏原型 · screen-{today,editor,settings} · R3 修改后再审 · 2026-08-22

- **用户意见原话（2026-08-22，三条）**：
  1.「最下面的周日、周一、周二啥的也删了吧」
  2.「目标编辑页面，那个更多图标跑到外面去了。『建好这个目标』改成『保存』。」
  3.「页面上不要有『数据只存在这台设备上』、『到了日子会提醒你问一声「怎么样了」，不会自动结束。』这些额外说明，显得很乱。要说明就好好描述，像一个APP一样，不要这种口语化的语言。」
- 结论：**修改后再审（R3 → R4 待复审）**；澄清问答 2 项（spec Clarifications 2026-08-22 第三场）——① 本地存储说明 = **彻底删除**（不迁移至设置页）；② 编辑器三个类型行为提示句 = **全删**（编辑器零行为说明句，语义由控件标签与到点通知文案承载）。
- 事实定性：反馈 2「更多跑到外面」为原型布局 bug——常用行 6×44px 图标 + 44px 更多 + 6×8 gap = 356px > 内容区 308px，more 右缘 397px 超出画板 390px（Playwright 量测）。反馈 1/3 为设计裁决：今日页再无任何聚合元素；FR-021 文案语域（正式 App 语域）入规格。
- R4 修订内容（三屏过 Playwright 实测；回顾屏无涉）：
  1. **今日页周节奏微条移除（反馈 1）**：CSS/模板/WEEK_PAST/scaffoldWeek/render 周格联动全删——今日页 = 头部带 + 目标卡列表 + 空态，今日之环（R2）与周节奏（R3）相继退役，历史节奏在回顾页查看。今日屏版本 R6→R7。
  2. **编辑器修正（反馈 2）**：常用行收纳 36px 图标 ×6 + 40px 更多 + 8px gap = 304 ≤ 308px——实测 more 右缘 345px、无横向滚动、四画板一致；弹窗内图标保持 40px 不变。保存按钮「建好这个目标」→**「保存」**（创建/编辑统一），save-sub 整行删除。
  3. **文案语域清查（反馈 3，FR-021）**：编辑器删 短期/习惯/长期三句行为说明 + 分类 hint「常用在这儿选，不选也行」+ 提醒子标签「按节奏轻轻叫一声」→「按频率定时提醒」+ placeholder「一句话说清它，比如…」→「例如：…」；今日页空态改「还没有目标 / 点击右上角 ＋ 创建第一个目标」、详情 sheet 标签「这次做了什么？（选填）」→「描述（选填）」、删 sheet 内原型示意句；我的页删账号卡「只存在这台设备上」尾注与「隐私」整行（本地存储说明任何界面不出现，用户裁决 A）。保留：到点通知「到日子了，怎么样？」（用户裁定通知文案）、通知空态友好语（FR-005 验收项）、演示记录文本（用户内容非界面文案）。
- 涟漪：spec.md（裁决 3 子节 + FR-011 保存文案 + FR-020 扩周节奏 + 新 FR-021 + US1 验收 7 扩）、quickstart.md（阶段 A 验证点 +2）、tasks.md（T017 周节奏移除/空态语域、T023 保存+零说明句、新增 T045 文案语域清查——lib/core/copy.dart 已知违禁 onboardingDataNote/privacyFoot/reminderNudge）。
- 待用户复审：R4 三屏（8390 原服务不变；回顾屏维持 R2 已过版）。
- **R4 复审结论（2026-08-22）：通过**——用户原话「好的，现在评审完成了」，无新增反馈。T007 评审门禁就此通过：四屏（今日 R7 / 编辑器 R3 / 回顾 R4 / 我的 R3）冻结为实现基准，T015 起 Flutter 结构任务解锁。

### 003 四屏原型 · screen-{today,editor} · R2 修改后再审 · 2026-08-22

- **用户意见原话（2026-08-22，两条）**：
  1.「新建目标页，图标很不错，但图标目前看应该算是目标的类型了，换个名称，另外交互这，部分放几个常用的显示在一行，其他折叠，然后可以点击更多，从一个新的悬浮弹窗里面选；另外几个选项也不需要有折叠的设计了」
  2.「今日页面，卡片上的按钮去掉吧，点击卡片就可以弹出记录的页面；然后卡片的介绍：昨天 - 然后最新一次记录的描述（这里对应着的记录的时候也需要写一段话来描述，如果不填可以有默认的）；今日之环这个卡片，去掉吧，还没想好怎么展示比较好。」
- 结论：**修改后再审（R2 → R3 待复审）**；澄清问答 3 项（spec Clarifications 2026-08-22 第二场）——① 图标区更名「**分类**」（与既有「目标类型」长期/短期/习惯撞名，故不叫类型）；② 打卡未填描述默认文案 =「**完成打卡**」；③ 常用行构成 = **固定策展约 6 枚高频图标，版本内置**。
- 设计定性：反馈 1 部分反转 R1 裁决 1（十域平铺行 → 常用行+弹窗）、反馈 2 废止 R1 裁决 3 相应条款（卡上单按钮保留 → 整卡可点；环卡收紧 → 环卡移除）——spec 中以新裁决子节显式声明废止关系。反馈 2 的「记录时写一段话」是新数据需求 → **schema v3→v4**（CheckIns +note TEXT NULL，纯 ALTER TABLE 增量；备份格式同步 v4 双向宽容）。
- R3 修订内容（今日/编辑器两屏过 Playwright 实测，回顾/我的无本轮改动）：
  1. **编辑器分类区（反馈 1）**：更名「分类」置顶——常用一行固定 6 枚策展图标（健身/读书/爱心/冥想/画笔/储蓄）+「更多」虚线钮 → 悬浮弹窗全量 38 枚按十领域分组（scrim/✕/Esc 关闭），两处选中态实时同步；基础信息/目标类型两组**全部平铺**（`.grp-static`，折叠控件全删）。实测：弹窗 10 域 38 枚、常用↔弹窗选互相切换选中态双向同步、Escape/scrim/✕ 三路关闭、无任何折叠控件。
  2. **今日卡整卡可点 + 描述行（反馈 2 前半）**：卡上「记录」按钮删除（.st-go/st-side 全清），整卡 role=button 可点/可键盘进目标详情 sheet；最新行改「**相对时间 - 该次描述**」格式（今天/昨天/N 天前 - 描述），详情 sheet 新增「这次做了什么？（选填）」输入——填写则入行、留空兜底「完成打卡」、无任何记录「还没有记录」。实测：带描述记一次 → 「今天 - 今天很放松」；留空记一次 → 「今天 - 完成打卡」；未记目标显示「昨天 - …」；4/4 后成就时刻照常触发。
  3. **今日之环移除（反馈 2 后半）**：hero 卡 + RING_CIRC 环渲染 + 统计三行 + 环动效标注全删，节头（今日目标）直接承接头部带；周节奏微条、节头计数「已记录 x/n」、空态/深色画板不受影响（实测五画板无回归）。聚合展示形态待后续设计，本轮不替代。
- 涟漪：003 spec.md（裁决 2 子节、US1 验收 6/7、US2 验收 2/6、FR-011/015 改写 + FR-019/020 新增、Key Entities CheckIn.note、Edge Cases 描述兜底/超长截断）、data-model.md（v4 + 迁移）、contracts/backup-format.md（v4 重写）、quickstart.md、tasks.md（T017/T021/T023/T026/T044 新增——T044 = CheckIns note 列 + 备份 v4 + 迁移对账用例）同步更新。
- 待用户复审：R3 今日/编辑器两屏（8390 原服务不变；回顾/我的维持 R2 已过版）。

### 003 四屏原型 · screen-{today,editor,review,settings} · R1 修改后再审 · 2026-08-22

- **用户意见原话（2026-08-22，四条）**：
  1.「图标这部分，占的太多了，一个类型一个平铺滑动就行，并且应该放在最上面；不要这种下拉效果，平铺能正常分清就行」
  2.「我的页面通知那块的设置都叠起来了，看看只是展示的问题吗」
  3.「今日页，卡片还少了最新进度，去掉今日x次，留下一个按钮，进入到记录页面就行；今日之环这个卡片的大小和里面组件的大小，调整一下，看着不协调，环里面不需要有内容。」
  4.「现在的目标图标我一个都没看着，是还没开发吗？」
- 结论：**修改后再审（R1 → R2 待复审）**；澄清问答 1 项——「记录页面」裁定 = 复用目标详情页（spec Clarifications 2026-08-22）
- 事实定性：②④为原型 bug 非设计问题——② `.gl-scroll` 纵向 flex 子项被压缩（通知卡 345px 压至 132px，与下方「目标」卡互叠；T004 回顾屏当时修过同款，settings/goals 漏修）；④ goal-icons.js `gi()` 误设 `viewBox="0 0 24 24"`，而内嵌 Material Symbols path 为 0..960/-960 网格 → 全部画在视口外。图标库本体已建成（T008，38 枚 + 单测），非未开发。
- R2 修订内容（四屏全过 Playwright 实测）：
  1. **编辑器图标区重构（反馈 1）**：图标卡置顶（静态头、无折叠/下拉），十领域每域一行「域名 3em + 横向平铺滑动条」，44px 图标钮即见即选；基础信息/目标类型两组保持可折叠居后。实测卡序 = 图标→基础信息→目标类型，10 行无溢出（390px 宽 3–5 枚/行均放下）。
  2. **我的页叠放修复（反馈 2）**：`.gl-scroll > * { flex-shrink: 0 }`（screen-settings + screen-goals 同步）。实测展开「按目标提醒」后通知卡 347/345px 不再压缩、四卡两两零重叠、scrollHeight 恢复滚动语义；分组结构未动。
  3. **今日页卡片 + 今日之环（反馈 3）**：目标卡 = 图标 + 标题 + 类型徽章 + **最新进度行**（「最近 · 今天/昨天/N 天前」，T014 copy 同款）+ 单一「记录 ›」按钮——计数 pill 与卡上快捷打卡全删；环卡 padding space-5→4、环 130→96px（r38 stroke8）、**环内文案全清**，语义由环色与右侧三统计承载。原型以新增 detail sheet 示意按钮目的地（详情页打卡/补签/历史），「记一次」后最新进度/环/徽章联动。
  4. **图标渲染修复（反馈 4）**：`gi()` viewBox 改 `"0 -960 960 960"`（Material Symbols 原生网格）+ 用法注释同步；今日页卡图标、编辑器十行图标、我的页嵌套提醒图标实测 bbox 全部可见。
- 涟漪：003 spec.md 已随裁决更新（FR-011 置顶平铺措辞、US1 验收 6/7/8 新增、US2 描述与验收 2/6 改写、Clarifications 双记录）；Flutter 侧 T017（今日卡）/T021（详情动线）实现口径以裁定 A 为准——卡上唯一按钮直达既有目标详情页。
- 待用户复审：R2 四屏（8390 原服务不变）。

### 三屏合并裁决 · 目标列表 R2 / 周回顾 R3 / 设置 R2 · 通过 · 2026-08-21

- **用户意见原话（2026-08-21）**：「行吧，先这样继续了」
- 结论：**目标列表 R2、周回顾 R3、设置 R2 一并通过——对应实现门禁解锁**（goals_view 列表卡语言返工；T021/T023/T024；T026/T027/T029/T030）。
- 口径说明：裁决前待审面恰为上列三屏 + 品牌图标三案；「先这样继续」对三屏按「通过」记（用户明确知晓四道门并给出继续指令，非对某屏的含糊修改意见）。**品牌图标三案不在本次通过范围内**——该门禁需要明确的选案（或混搭）裁定，「先这样」未构成选案，T028 生成链继续挂起。
- 连带：周回顾 R3 条目中「待用户裁决：纯查看口径是否成立」一并落定——成立；补签功能（今日屏长按）保留口径随「先这样」默认维持现状。

### 周回顾 · screen-review.html · R3 通过 · 2026-08-21

- **用户意见原话（2026-08-21）**：「周回顾这里就不用控制每周需要干什么了，聚焦查看的内容。不需要有补签的状态，甚至忙碌的状态也可以去掉了（其他页面也去掉），不用设置这么多。另外有些细节注意下，现在的卡片好像还能向下滑动。」
- 结论：**R3 修订，待评审**（R1/R2 的决策动线语义随本次裁决废止）
- R3 修订内容：
  1. **决策动线全删**——「下周怎么走？」三选胶囊（继续/调频/暂停）、调频 chips、「新频率下周一生效」、一句话回顾（这一周什么帮到了你）与「记下这一周」保存按钮全部移除。教练语改**观察语**：只描述事实（「这周只在周六下了一次水，连着两周在降」），不再提问或建议（原「要不要把频率调小一点？」已改）。屏成为纯回看：周摘要 + 图例 + 横滑逐目标卡；要调整去目标页找它。
  2. **补签状态不上回顾屏**——「补」角标、「后来补上」图例项、「补签 N 次」徽标、补签透明脚注与整个补签画板删除；节奏条只剩实心/空圈/不适用三态。今日屏长按补签功能本身保留（用户裁决针对回顾屏的状态展示；如需一并移除待示下）。
  3. **忙碌态全 App 移除**——本屏忙碌画板/横幅/徽标删；screen-goals.html 忙碌 chip「忙碌中 · 最低档」×2 删（潜水卡元行只留「假期练」）；screen-today.html 忙碌画板删（六板→五板，序号顺移）、忙碌横幅节点与 CSS、问候语「忙碌的一天」分支删。direction-*.html 为已否决留档，不改。
  4. **「卡片还能向下滑动」根因修复**——实测定位：`.gl-scroll` 为纵向 flex 容器，内容超高时 `min-height:auto` 只豁免 `overflow≠visible` 的子项，pager 独自被压缩（419→327px）；又因 `overflow-x:auto` 使 `overflow-y` 计算值为 `auto`，压出的 92px 溢出可被拖滑。双刀封死：`.rv-pager { flex-shrink: 0; overflow-y: hidden }`。验证：强设 `scrollTop=50` 弹回 0、纵向 delta=0、横滑与圆点联动回归正常。
- 画板收敛：① 典型周（可交互，四卡横滑）② 深色并列（两卡）。
- 实现取值：100% token（SC-004）；Playwright 实测——两画板卡/圆点 4-4、2-2 配对，整页零横向溢出。
- **涟漪（实现期任务已同步改写 specs/002 tasks.md）**：T021 review_view 重写不再含决策动线；T022 由「忙碌视觉区分」改为「忙碌模式全 App 移除」（busy_mode_view 收敛、今日屏忙碌态、列表徽标、提醒降档）；今日屏已实现的忙碌分支将随 US4 移除任务拆除。
- 待用户裁决：纯查看口径是否成立；补签功能本身的去留（本屏已无状态展示）。

### 品牌图标 · A 案「今日之环」选定 + 生成链通过 · 2026-08-22

- **用户裁决原话（2026-08-22）**：「1.选择A 2.侧载成功，可以归档，更新下相关文档」——三案裁定 **A 今日之环**（App 自己的仪表语言升为标识）；同条确认真机侧载成功（T033 归档，见实现审计）。
- **母版落地**：`design/brand/icon-a-light.svg` / `icon-a-dark.svg`（1024×1024，几何 1:1 派生自 proposal 的 markSVG('a')：环心 60,60 r34 stroke14、青柠 75° 弧自 12 点顺时针、靛蓝珠 94,60 r9；浅底 = bg-grad 四档 135°，深底同构暗紫四档；不透明全出血方图，squircle 蒙版交系统）。
- **渲染与像素级核验**：Chromium 1024² 直渲 PNG，自写 PNG 解码器（zlib + 逐行反滤波）逐点采样对照几何——浅版四角 #ebd7e0→#d2ccf7 对角渐变、环左中带 #252230、环顶/弧中 #b5e550、珠心 #5b6ab0 全中；深版同构（环身 #f2eff7、珠 #9aa5e0、四角 #2b1f35→#130e1d）。
- **过程坑（留档）**：light 版首渲全白报废——SVG 注释里写了 `--bg-grad-1..4`，**双连字符在 XML 注释中非法**，Chrome 给的是 XML 错误页；视觉模型读「无图形」为真、像素探针定位根因，改写注释后复验通过。教训两条：SVG 注释禁用 `--`；视觉读数与源码矛盾时用仪器仲裁，别急着归为误读。
- **生成链（T025 门禁后执行）**：pubspec.yaml 增 `flutter_launcher_icons`（ios only / remove_alpha_ios / 其余平台 false）与 `flutter_native_splash`（color + color_dark、android/web false）配置块；`dart run flutter_launcher_icons` + `dart run flutter_native_splash:create` 全绿——AppIcon.appiconset 全档重生成、LaunchImage 浅/深双套（appearances luminosity dark）+ LaunchBackground 1px 拉伸层 + LaunchScreen.storyboard 全出血重构、Info.plist 增 `UIStatusBarHidden`（其余为重排）。
- **iOS 18 深色图标变体（手工补）**：AppIconDark.png + Contents.json 单尺寸 1024 universal + dark appearances 条目；染色（tinted）变体未做，iOS 18 回退深色，可后续补。
- **启动屏纯色近似口径**：LaunchScreen 不支持渐变，底色取四档均值——浅 #ded2ed / 深 #1f172a；前景 640×420 透明 PNG（220px 环 + 「生活目标守护」44px / .3em 字距），contentMode center 全出血不裁切（640/2=320pt 恰合最小屏宽，文本任何机型不出血）。
- 验证：`flutter analyze` 0 issue + 69 测试全绿；**真机生效需再手动触发一次 Codemagic 构建 + 侧载**（当前装机仍是旧图标）。

### 品牌图标 · brand/icon-proposal.html · R1 候选送审 · 2026-08-21

- **触发**：T028 品牌母版——设计语言已随今日屏 R4 定稿，图标方向可先行送审；**只裁方向与母版语言，生成链（flutter_launcher_icons / native_splash）仍守 T025 评审门禁后执行**。
- 三案并陈（每案 浅/深双版 × 160/60/29 三级尺寸实读 + 启动屏预览 + 主屏混排语境）：
  1. **候选 A「今日之环」**——App 自己的仪表语言升为标识：墨梅环身（--accent）+ 青柠今日弧 75°（--positive-fill）+ 靛蓝记录珠（--goal-indigo）。与站内圆环同构，零断层；稳妥之选。
  2. **候选 B「墨梅一瓣」**——accent 令牌本名「墨梅」，寒冬自开的坚持；五瓣圆钝几何、顶瓣（今日）青柠，「把今天照顾好一瓣」。故事最完整、最难撞脸。
  3. **候选 C「今字印」**——印章式宋体「今」字：一切只发生在今天。中文单字图标在 iOS 海洋里几乎不撞脸；风险是只在中文语境自证（海外需另配字标）。
- 实现取值：全部派生自 tokens.css（环身/字墨 = accent 浅 #252230 深 #f2eff7；青柠 = positive-fill；靛蓝珠 = goal-indigo 浅深双值；底幕 = bg-grad-1..4 135deg）；iOS 资产需实体色值，令牌为真源。iOS 18 染色变体三案结构均适配。
- Playwright 实测：24 图标 + 5 干扰项全渲染、方形成立、svg 与盒同径、整页零横向溢出；截图自审三级尺寸可读性（29px 下 B 花形与 A 环形仍可辨，C 字形为块状剪影——真机 29pt 恒为 2x/3x 物理渲染，实际优于本页 1x 模拟）。
- 待用户裁决：选案 / 混搭裁定（如「B 的花 + A 的环语言」）、启动屏是否带字标。

### 目标列表 · screen-goals.html · R2 通过 · 2026-08-21

- **用户意见原话（2026-08-21）**：「我看目标列表已经是审核过的啦，那应该是我之前漏掉了，目标列表这个页面不符合要求，差太多了，重新设计一下。」
- 结论：**R1 通过作废（用户补审否决）→ R2 整屏重做，待评审**
- R1 → R2 差距诊断与修订：
  1. **演示数据换真实四目标**——R1 用虚构目标（每天散步/好好吃饭/睡前读 5 页），与今日屏 R4、周回顾屏不一致；R2 全部换用户真实目标（第一个 App / 骑行大 PRO 计划 / 潜水证 OW/AOW / 冈仁波齐徒步），图标、目标色、「最近 · ×××」线索与回顾屏同源。
  2. **去掉每日打卡微环**——R1 卡右侧是「1/3、0/1」今日打卡进度环（打卡是今日屏的职责，且是 R2 意见 8 语义转向前的完成率口径）；R2 改为本周节奏「N/7 天有记录」tnum 数字（记录语言，与回顾屏 rv-ratio 同语法）。
  3. **卡结构对齐今日屏 R4**——三行玻璃卡：row1 目标色 40px 图标 + 名称 + 右缘节奏数；row2 为什么（B 案动机句，缺省 → 虚线「＋ 补一句为什么」渐进补全）；row3 元行 chips（频率 / 场景 / 忙碌·最低档徽标）+ 右对齐「最近 · ×××」。
  4. **顶下小结**「4 个目标 · 本周留下 10 次记录」（努力语言）；一次性 · 截止日目标同列带徽标不分区（FR-011）；暂停区虚线折叠行 + 恢复保留。
  5. **导航对齐 R4 定稿**：常驻四页签（目标选中 chipx），弃浮动 3-tab。
- 画板：① 典型（四真实目标 + 缺为什么 + 忙碌徽标 + 暂停行）② 空态（提问 + 模板 + 写一句自己的）③ 深色并列。
- 实现取值：100% token（SC-004）；Playwright 实测——三画板 phone/gl-scroll 零横向溢出、nav 四页签等分 84px、卡 342px 内 ratio 距右缘 17px、为什么单行 clamp、最近行 ellipsis、深色忙碌徽标自动转 warning #e8b04b。
- 待用户裁决：卡内信息密度（为什么 + 最近是否都在卡上）、节奏数字径、忙碌徽标口径。

### 周回顾 · screen-review.html · R2 修订送审 · 2026-08-21

- **用户意见原话（2026-08-21）**：「周回顾，每个目标的通过左右滑动卡片来展示。」
- 结论：**修订完成，待评审（与 R1 送审合并裁决）**
- 修订内容：
  1. 每个目标一张卡，**左右滑动切换**（scroll-snap `x mandatory`，卡全宽出血滚动）；周摘要、图例、「这一周，什么帮到了你？」与「记下这一周」保持共享常驻，不在卡内。
  2. **目标色指示点**在 pager 下方（当前目标色放大 1.4×，未选 divider 色），可点按跳转（smooth scroll）；滑动与点按双向联动。
  3. 四画板全部改造（4/3/3/2 张卡对应各画板目标集）；决策 ≤3 步、节奏条、教练语、忙碌/补签语义均未动。
- 实现取值：Playwright 实测——四 pager 卡宽 342、snap x mandatory、滑动到第 3 张指示点正确跟随、点第 4 点滑到 index 3、外层 phone/gl-scroll 零横向溢出（pager 内部 354×N 为预期分页宽度）、决策交互（调频/暂停展开互斥）回归全过。
- 待用户裁决：滑动卡模式下的节奏条/决策区留卡内（逐目标独立决策）是否成立；指示点形态。

### 设置 · screen-settings.html · R2 通过 · 2026-08-21

- **用户意见原话（2026-08-21）**：「设置里面就不需要展示目标的内容了，聚焦APP本身的设置。」
- 结论：**修订完成，待评审（与 R1 送审合并裁决）**
- 修订内容：
  1. **逐目标提醒四行整体移除**（第一个 App / 骑行 / 潜水 / 冈仁波齐行全部删除）——目标提醒时刻回归编辑器场景选择（B 案 cue_scene），设置页不展示目标内容。
  2. 提醒组只留「今天的小事」概要（08:00 + 开关）+ 两条 hint：周一概要带上周回顾；「目标提醒的时刻在编辑目标时选——早起后 / 午休时 / 晚饭后 / 睡前；没选的 20:00 轻提醒，同一时段几个目标合并成一条」（FR-012 空值回落 + 同档合并的口径保留在文案层）。
  3. 身份卡副行「4 个目标 · 本周 10 次记录」删除（属目标内容），只留头像 + 名字。
  4. 备份与数据组、V7 冲突弹层、隐私一句话脚注、权限未开启画板、深色画板、常驻四页签导航均未动。
- 实现取值：Playwright 实测——目标内容残留清零（goal 色引用与目标名均无）、三画板零横向溢出、开关剩 1、弹层开/关回归过。
- 待用户裁决：聚焦后的分组密度；场景指引 hint 是否保留。

### 设置 · screen-settings.html · R1 送审 · 2026-08-20

- **「我的」页签 · 三画板 · R1 · 2026-08-20**
- 用户意见原话：（待评审）
- 结论：**待评审——T026+ 门禁：通过前不重写 `lib/features/settings/settings_view.dart`（T026–T030 均被本轮阻塞）**
- 送审内容与评审要点：
  1. **页面定位**：「我的」= 身份 + 提醒 + 备份，不再是系统设置清单。顶部身份卡（渐变头像 + 星行 + 「4 个目标 · 本周 10 次记录」努力语言，与今日/回顾屏同一口径，无完成率）；页尾虚线脚注一句话讲完隐私「数据只在这台设备上——不上传、不联网，记得定期导出备份」（FR-014 降为一句话，不吓人）。
  2. **提醒组按 FR-012 联动预演**：「今天的小事」概要行（08:00 + 周一概要带上周回顾 hint）；逐目标提醒行 = 目标色图标 + 名称 + **场景徽标（晚饭后/睡前/早起后/不打扰）+ 场景对应时刻**——时刻由编辑器 B 案的 cue_scene 驱动，「不打扰」显示「—」且开关默认关；hint「没选场景的目标 20:00 轻提醒；同一时段几个目标，会合并成一条」（空值回落默认时段 + 同档合并打扰，T027 实现的验收口径）。
  3. **备份组 + V7 冲突弹层可交互**（画板一点「导入备份」实弹）：标题「导入会覆盖当前全部数据」/「两份不会混合——继续将替换为备份内容」/ 覆盖本地（accent 实心）+ 取消；导出行 sub「生成一份文件，带走全部记录」口语化。开关均可拨。
  4. **权限被拒不劝留**（画板二）：「通知未开启」卡说明一次「不开通知也能用全部功能；想开的话，去系统设置里找 Target」+ 开启通知/知道了，其余开关照常展示（偏好先记住）——FR-007 不反复弹窗的设置侧呈现。
  5. **深色画板**（③）：全部取值语义 token，`[data-theme="dark"]` 作用域自动切换（surface 条/墨梅→反白胶囊/青柠开关），无逐条覆盖。
  6. **导航对齐修正（跨屏一致性）**：本屏与周回顾屏的底部导航照抄今日屏 R4 定稿语言——常驻 `.st-nav` 条（surface 底）+ 四页签 flex 等分（今日/目标/回顾/我的）+ 选中 `.chipx` 墨色内胶囊（图标上文字下）；此前 review 原型误用了浮动 pill 且注释自称「R4 同语言」，本轮一并修正并给周回顾补上「我的」第四页签互链（今日屏 R4 已四页签，实现层 router.dart 同构）。四画板 nav 实测等分 84px×4、零溢出。
  7. **实现取值**：100% 取自 `../tokens.css`（SC-004）；Playwright 实测——三画板 phone/gl-scroll 双层零横向溢出（修正前浮动 pill 414px>390 溢出 12px，改为常驻等分条后归零）、玻璃卡浅 0.8 白/深 0.09 白、开关 on 青柠 #b5e550、场景徽标/时间 tnum、弹层水平居中 Δ<2px、开关 ×5 拨动还原、弹层开→取消/覆盖本地双径关闭、深色 chipx 反白 #f2eff7 墨字 #1a1622、nav 条深色 #241e33（token 自动）；视觉模型审查的「缺行/截断」指控经 DOM 实测为滚动视口裁切，非缺失。
- 待用户裁决：身份卡努力语言口径、场景徽标+时刻的 FR-012 呈现、隐私一句话脚注；通过即解锁 T026（settings_view 重写）。

### 周回顾 · screen-review.html · R1 送审 · 2026-08-20

- **周回顾 · 四画板 · R1 · 2026-08-20**
- 用户意见原话：（待评审）
- 结论：**待评审——T021 门禁：通过前不重写 `lib/features/review/review_view.dart`（US4 T021–T024 均被本轮阻塞）**
- 送审内容与评审要点：
  1. **语义转向落地（R2 意见 8 的 US4 承诺项）**——回顾屏不再出现「完成率」百分比：数据翻译为「留下 N 次记录 · N 个目标」的周摘要 + 每卡「N/M 天有记录」。签名元素 **周节奏条**：一周七格（实心=有记录、空圈=没记录、中心小点=不适用、「补」角标=后来补上），顶部一次图例教完全部语法。
  2. **近 4 周趋势**：目标色透明度阶梯柱（.35/.55/.75/1），教练语按节奏分三档——稳「节奏很稳，照这个走就好」/ 中「有几天被挤掉了，正常的一周」/ 低「要不要把频率调小一点？」（warning 色非红、建议式不评判）。
  3. **决策 ≤3 步实演**（画板一可交互）：三选胶囊（继续/调频/暂停）1 击 → 调频展开频率 chips 第 2 击即定 → 「新频率下周一生效」脚注常驻；暂停展开温和语「记录都保留，随时回来」。演示目标沿用真实四目标（第一个 App / 骑行大 PRO 计划 / 潜水证 OW/AOW / 冈仁波齐徒步）与「最近 · ×××」努力线索行。
  4. **忙碌周画板**（②）：周摘要改「忙碌的一周 · 4 个目标各记了一次」+ 横幅「忙碌周 · 目标只记最低档，也算数」+ 每卡忙碌徽记，教练语换不评判口径；徒步卡演示「暂停」展开。
  5. **补签透明画板**（③）：补上的日子在节奏条带「补」角标（与当天记录不混淆），卡上「补签 N 次 · 周几晚上补上」徽记 + 虚线脚注「补上的也算数，只是如实记下时间」；徒步卡全当天记录作对照。
  6. **深色画板**（④）：暗紫底幕玻璃卡同语言静态并列。
  7. **实现取值**：100% 取自 `../tokens.css`（SC-004）；Playwright 实测——底幕 135° 四锚点渐变、玻璃卡 0.8 白/radius 16、节奏条七格中心 Y 恒等（Δ0）间距 53.6–53.7 均匀、走势柱四根同底线、三选按钮 116px 等宽等距、「补」角标 warning 色压格、胶囊选中墨梅 #252230、深色卡 0.09 白、四画板零横向溢出；交互链 调频→chips→暂停→继续 全过；视觉模型审查的可疑项经几何实测全部排除（其对色值/数量的描述与实现不符）。**修订附注（08-20，随 T025 送审同步）**：底部导航由浮动 pill 改为今日屏 R4 定稿常驻等分条并补「我的」第四页签（原注释自称 R4 同语言但结构不符；决策区等屏内内容未动，交互回归全过）。
- 待用户裁决：节奏条语法、教练语三档口径、决策展开动线；通过即解锁 T021（review_view 重写）。

### 编辑器 + 目标列表 · screen-editor.html / screen-goals.html · R1 通过 · 2026-08-20

- **编辑器（定义模型三案）+ 列表语言 · R1 通过（用户选定方案 B）· 2026-08-20**
- 用户意见原话：「选B」
- 结论：**通过——定义模型定稿为方案 B「为什么」，T014 门禁解锁（T015/T016 开工）**。选定字段集（data-model envelope，全部可空 ADD COLUMN）：
  - `motivation`（动机）：创建时必填一句（UI 层约束，非 DB 约束），≤60 字；NULL → 今日卡/列表卡出现「补一句为什么」渐进补全入口
  - `success_criterion`（成功标准）：按名称 SMART 自动拟好、可改，≤60 字；非空 → 打卡反馈优先展示
  - `cue_scene`（提醒场景）：chips 选填（早起后/午休时/晚饭后/睡前/不打扰），≤40 字；入选 → 驱动该目标提醒时刻（FR-012），空/不打扰 → 回落默认时段、同档合并
  - 方案 A/C 未选字段（最小行动等）**不入库**；打卡模型沿用 001（count/target 频率制 + 努力记录语言，R2 语义转向已由 US2 落地，无 schema 影响）
- 动线说明：三案共享结构（模板一句话起步、SMART 内联建议、一次性/截止日属性开关无类型分叉 FR-011、渐进补全、图标颜色）与列表页四形态卡语言随选案一并通过；无修订意见。
- 修订说明：无需修订（R1 直接过）。

### 编辑器 + 目标列表 · screen-editor.html / screen-goals.html · R1 送审 · 2026-08-20

- **编辑器（定义模型三案）+ 列表语言 · R1 · 2026-08-20**
- 用户意见原话：（待评审）
- 结论：**待评审——T014 门禁：选定字段集与动线通过前，禁止动数据库 schema（契约 §3-3）**
- 送审内容与评审要点：
  1. **定义模型三案（本次评审核心裁决点）**——同一个创建动线，三种「一个目标包含什么」的答案，三案共享模板路径 / SMART 内联建议 / 名称+频率+一次性属性开关（FR-011，不前置选类型）/ 图标颜色，差异只在「深化字段」：
     - **方案 A「慢慢来」**：深化字段全选填后补。最快开始（30 秒），但多数目标可能永远停在「名称」，模型价值打折；
     - **方案 B「为什么」（推荐）**：动机必填一句 + 成功标准按名称自动拟好可改 + 提醒场景 chips 选填。今日卡第二行回显动机、打卡确认用成功标准文案、提醒文案含动机（FR-012 全链路联动），代价约 +10 秒；
     - **方案 C「小到不会失败」**：场景锚（if-then）——提醒时刻必选 + 最小行动（成功标准变体）。行为科学落地性最强、提醒时机不用另想，但对里程碑型目标表达别扭、动机维度缺席。
     - data-model envelope 按契约：**只实现选中方案的字段**（motivation ≤60 字 / success_criterion ≤60 字 / cue_scene ≤40 字，全可空 ADD COLUMN），未选字段不入库。
  2. **画板**：① 创建（可交互，A/B/C 按钮即时切案，「演示 SMART」重现模糊名→内联建议→一键采用，一次性开关/步进器/chips 全可点）② 既有目标渐进补全（001 迁移目标的新维度虚线入口，点开成内联字段，不强制不阻塞）③ 深色并列（B 案定妆示意）。列表页另有典型（四形态卡：有动机回显 / 待补全邀请 / 忙碌徽记 / 暂停折叠行）/ 空态（模板一句话起步）/ 深色三画板。
  3. **实现取值**：两页 100% 取自 `../tokens.css`（SC-004），交互演示与深色画板经 Playwright 实测（切案换文案、SMART 采用、开关/步进、ghost 展开、深色计算样式全过）。
- 待用户裁决：三案选一（或融合）；字段集定稿即解锁 T015/T016（可空列迁移 + 备份扩展）。

### 今日屏 · screen-today.html · R4 通过 · 2026-08-20

- **今日屏 · R4 通过（用户裁决今日屏收口）· 2026-08-20**
- 用户意见原话：「行了，这个首页我先审完了，主要的设计思路和语言应该都有了，你可以继续推进了，我要去睡觉了，如果有什么需要审批的明天再说，尽量往前推进，之后提交就行。」
- 结论：**通过——今日屏设计语言定稿（T008 门禁解锁，US2 T009–T012 开工）**。口径说明：用户明确表示"审完了 + 可以继续推进"，非含糊表述，按契约 §4 记"通过"；"先审完"的含义为设计语言层面定稿，后续屏幕深化过程中用户若再提首页微调，随该屏评审一并处理。
- 通过版本 = screen-today.html R4（含追加渐变可见化）：年度守护卡已移除；底部导航 = 白色全圆角胶囊条（inset 与内容列对齐、图标 20 等大、选中墨色胶囊）；浅色 = 可见粉→紫 135° 底幕（#ecd8e0→#d1ccf8）+ 墨梅 accent #252230 + 弥散软影；深色 = 暗紫底幕 + 反色强调（未动）。

### 今日屏 · screen-today.html · R4 · 2026-08-20

- **今日屏 · 六画板 · R4（用户对 R3 版的意见，红框标注截图）· 2026-08-20**
- 用户意见原话：「稍微好点了，但是还有：1.年度守护这个卡片直接去掉吧，没用 2.导航条的圆角，和间距、大小还是看着有些奇怪，你再仔细看下图片吧，我用红框框出来了 3.浅色模式下的页面背景感觉看着不舒服，你能调调整个设计的颜色吗？包括今日进展、打卡按钮啥的」
- 结论：**修改后再审（R4 修订完成，待用户复审）**
- 修订说明（R4 版，三项全改）：
  1. **年度守护卡整体移除**：模板、`buildBand` 伪随机时间带及其调用、`.st-year*` 样式、壳层示意图节点全部删除（R1 轮由倒计时概念演化而来，用户裁决无用）。今日屏信息结构收敛为：问候顶栏 → display → 今日进展 → 今日目标（胶囊+目标卡）→ 底导航。
  2. **导航条按参照图重做（几何实测定位三处根因）**：① **左右错位 8px**——滚动区/卡片列内边距 24（space-6），导航容器却是 16（space-4），导航条比所有卡片每侧宽 8px；② **圆角歧义**——58px 高的条配 radius-lg 16，既非直角条也非胶囊；③ **图标不等大**——选中 20 vs 未选中 22。重做：白色悬浮**全圆角胶囊条**（radius-full），容器内边距改 `space-2/space-6/space-4` 与内容列严格对齐（实测 inset 24.0 = 24.0），四页签 `flex:1` 等分（中心距 [45.8→296.3] 恒差 83.5px），图标统一 20px，选中 = 墨色胶囊 54×42（图标上文字下），条内四周 8px 呼吸；条高 58/宽 342。
  3. **浅色配色整体调整（令牌级，双端同步）**：① **底幕再调淡**——四档 `#f9edf4/#f5e8f6/#f0e2f8/#e9dbf2 → #f9eff4/#f7ecf5/#f4ebf7/#f1eaf8`（末档薰衣草 #e9dbf2 过深压得页面发闷，向定稿参照的低饱和暖粉回归），均值 `#f1e5f4 → #f4eff6`；② **强调色去"死黑"**——`--accent #17181c（纯黑）→ #252230（墨梅）`，与参照的深灰墨一致；「今日进展」主卡、打卡按钮（未记录态）、导航选中胶囊、新建钮随令牌整体换色（已记录态仍青柠 #b5e550 对勾）；③ 文字/发丝线随族微调：`--on-surface #1d1a24`、`--on-surface-variant #625e70`、`--divider #e8e3f0`、`--surface-alt #f5f0f9`；④ **shadow-low 改弥散软影** `0 1px 3px → 0 2px 10px`（底幕变淡后白卡靠更柔的阴影分层，向参照卡片的轻阴影靠拢）。深色主题不动。
  4. **【追加修订（同日第二轮意见）】底幕渐变可见化**——用户追加意见原话：「背景能要那种渐变的效果吗？跟图里一样」。上述调淡版四档色差 ≤14、渐变几乎不可感（视觉模型评为「隐约可见」）。对参照图做画布像素采样（三部手机屏幕内部 3×3 分区取背景像素中位色 + 全图 48px 字符色图定位布局）：**屏幕顶部玫瑰粉 ≈#e9d3da、底部 periwinkle ≈#d6d0f7（R-B 摆幅约 40），135deg 对角**。直接按实测锚点重铸：`#ecd8e0/#e2d3e9/#d9d1f2/#d1ccf8`（均值 #ded3ed），替换调淡版。自渲染像素复核：左上 #e8d6e3 → 右下 #d4cef5，摆幅 ~38 与参照一致；正文对末档 11.2:1，`--on-surface-variant` 随之加深 `#625e70 → #565264` 保全段 ≥4.5:1（对末档实测 4.92）。白玻璃卡 0.8 白叠加后仍近白，粉紫仅轻微透色。深色主题不动。
- 契约变更：`contracts/design-language.md` §4 增 2026-08-20（R4）条目（追加渐变可见化与次级文字加深）；tokens.css 链接版本参数最终 `?v=0820f`（index.html 同步）。
- 验证：年度卡 DOM 已不存在；导航几何全实测（inset 对齐 24.0/24.0、胶囊 9999px 圆角、中心距恒等、图标 20/20）；六画板折叠复测——**首卡完整落于折叠线上方**（典型 -16.8 / 忙碌 -15.0 / 全完成 -16.8 / 深色 -14.8，负值 = 卡底在导航条上缘之上，较 R3 的 6.8–8.8 探出更优）；交互链复测（潜水卡点击 → #252230 → 青柠 #b5e550、圆环 326.7 走满、成就层触发、450ms 后复测非过渡假读）；渐变与对比度像素级复核（见第 4 条）；视觉模型通读无突兀项；`flutter analyze` 无告警。

### 今日屏 · screen-today.html · R3 自审 · 2026-08-20

- **今日屏 · 六画板 · R3 自审（设计主理人复查，用户 R3 评审前）· 2026-08-20**
- 缘起：R3 呈审后、用户裁决前，按「苛刻自审」纪律全量复查六画板（DOM 计算样式/几何实测裁决，视觉模型观察仅作线索）。
- 结论：**发现并修复 6 项**（1 严重缺陷 + 5 协调性），R3 继续待用户评审：
  1. **【严重】浅色板仪表卡文字隐形**：`.st-hero` 浅色为反色黑卡（--accent #17181c），但卡内二级文本沿用 `--on-surface`(#17181c)/`--on-surface-variant`(#5d5a66)——「今日进展」标题、三行统计的数字/标签/图标、圆环中心小注在黑底上对比度 1:1–1.9:1，近乎不可见（黑卡右半边看起来空了一块；深色板 hero 为深底幕卡故无恙）。修复：浅色作用域内 hero 就地反转文本令牌（`--on-surface→--accent-on`、`--on-surface-variant→accent-on 72% 透明度`）。实测修复后全部白/72% 白可读。
  2. **圆环中心数字与小注相隔 38.5px（视觉离异）**：`.center` 用 grid `place-items:center` 导致两个隐式行被拉伸平分、各自行内居中——数字悬上半、小注沉下半，中间空洞；reviews R3 声明的「4px 间距」与实际渲染不符。修复：flex column + justify-content:center，内容整体居中，实测间距回到 4px。
  3. **底部导航分布不均**：`space-around` + 选中胶囊变宽（62 vs 30px）→ 页签中心距 [95.5, 79.5, 79.5]px，首个视觉空隙 49.5 vs 后续 64.5px，切换选中态整排跳动。修复：`.st-tab{flex:1}` 等分槽位，实测中心距 [87.5, 87.5, 87.5] 恒等。
  4. **选中态图标反而更小**：胶囊内 svg 18px < 未选中 22px。修复：20px（胶囊内光学略收）+ 图标文字间距 2→3px。
  5. **头像渐变发闷（令牌级修订，契约变更记录 2026-08-20）**：文字级 goal-amber #c98a1b → goal-coral #d9534f 做 44px 装饰填充呈芥末棕→砖红，在回调后的粉紫底上显脏（定稿参照是明快暖琥珀）。新增语义令牌对 `--grad-avatar-a/b`（浅深同值 #eab54e→#ef8a80，填充级明度），tokens.css 与 design_tokens.dart（kAvatarGradA/B）一次提交内双侧同步。
  6. **空态保留死仪表盘**：无目标时仍显示「– / 暂无目标 / 0 / 0 / 0」黑卡 + 「今日目标」节头——新用户首页是一块全零面板。修复：空态隐藏 hero 与节头（胶囊已隐藏），首屏 = 问候 +「今天想做点什么？」+ 邀请卡 + 年度带。
- 实测排除的疑点（视觉模型误报）：＋/铃铛按钮尺寸一致（36/36）、头像与问候居中（±0.1px）、统计行层级存在（数字 700/标签 400）、图标与文字居中偏差 0、各卡「最近记录」无一截断（最长 167px < 可用）。
- 验证：六画板折叠全部复测通过（card1 完整在折线上 6.8–8.8px、card2 在折线下）；交互链 记录→计数+1→4/4→成就覆盖层 复测可触发；tokens.css 经版本参数 `?v=0820b` 防内存缓存；flutter analyze 无告警。

### 今日屏 · screen-today.html · R2 · 2026-08-19

- **今日屏 · 六画板 · R2 · 2026-08-19**
- 用户意见原话：「我现在每个都需要扣细节啊，尽量保证原型图能达到最好的效果。1.进度圆圈里面百分比、完成率的文字位置太贴到圆圈了。2.卡片的比例有点大了，感觉空隙很多 3.字体有单独设置吗？感觉设计不是很统一，有些文字粗细看起来很奇怪，字体也是 4.下面的导航条圆角是否能跟卡品或者其他的统一，还有选中的状态应该有BUG，今日在icon的右面；周回顾这个导航需要换成回顾 5.深色模式下，整个页面的背景看着很协调，但是浅色不是 6.卡片里面打卡按钮看不到按钮图标了 7."把今天照顾好"，"今天很忙，也没关系"，"早上好"这些标语都太生硬了，就正常一点的描述就行，其他的标语也都看看 8.这些目标都太简单了，我可以告诉几个我现在的目标：第一个APP、骑行大PRO计划，潜水证OW/AOW，冈仁波齐徒步；我的想法是其实不需要有完成度的概念，只需要记录下达到这个目标你做了哪些努力，比如潜水有第一次DSD、徒步有格聂南线完成等等 9.② 空态里面，创建目标，在当前屏里面显示不全」
- 结论：**修改后再审（9 项全改，R3 待评审）**
- 修订说明（R3 版）：
  1. **圆环中心文字**：中心文本框由整环盒收缩为 inset 14px 的内圆盒，数字行高 1、小注行高 1 + 4px 间距，四周留白恢复舒适（放大图复核确认不再贴环）。
  2. **卡片收紧**：内边距 20→16、行距 12→8、记录钮 36→32；首行补「节奏」字段（碎片时间/每周末/假期练/拉练期）消除右侧空档，底部行左移「最近 · 格聂南线完成」努力线索——信息密度上来后卡片高度 143→122。
  3. **字重二档化（令牌级修订，契约变更记录 2026-08-19 第二条）**：中文回退字体对 500/600 无真实字重、浏览器伪加粗是粗细怪异的根因——titleLarge/titleMedium 600→700、label 各档 500→400；屏内中文只用 400/700、数字 tnum 700、display 800；状态栏拉丁数字保留 Inter 真实 600。tokens.css 与 design_tokens.dart 双端同步。
  4. **导航**：圆角 24→radius-lg 16 与卡片统一；选中态改为黑色胶囊内**图标在上文字在下**（与未选中一致的方向，仅加胶囊底）；「周回顾」→「回顾」。
  5. **浅色底幕回调（令牌级修订，同上变更记录）**：bg-grad 四档 `#f5e6f5/#ede0f8/#ddd0f0/#ccc0e8` → `#f9edf4/#f5e8f6/#f0e2f8/#e9dbf2`（薰衣草饱和度降回定稿参照的透气粉紫），background 均值同步 → 深浅两版的底幕协调度对齐。
  6. **记录按钮图标**：未记录 = 黑底白色＋号（此前对勾描边动画在未完成态不显示任何图形）；记录后 ＋ 号切对勾并播放描边动画。深色下未记录为白底黑＋。
  7. **文案全面口语化**：问候「早上好，星行」+ 日期行「8月19日 星期三」；大标题 典型「今天做了什么？」/ 忙碌「忙碌的一天」/ 全进展「每个目标都有进展」/ 空态「今天想做点什么？」；横幅「忙碌模式 · 今天只需记录一次努力」；空态卡「记录第一个努力」；徽章「每个目标都有进展 · 今日 N 次记录」。
  8. **努力记录模型（产品语义输入，原型先行落地）**：目标没有完成度，只有努力记录——演示目标换为用户的真实目标（第一个 App / 骑行大 PRO 计划 / 潜水证 OW/AOW / 冈仁波齐徒步），每卡显示「最近一次努力」（完成第一版交互稿 / 均速突破 25km/h / 第一次 DSD 体验潜水 / 格聂南线完成）；圆环语义由「完成率」改为「今日有进展的目标 3/4」，统计行 = 今日行动 / 连续天数 / 进行中目标；胶囊 =「今日 N 次记录 / N 个目标」；年度带图例改「有记录/未记录/今天」。**注：此为 US3 目标定义模型（T013/T014 字段集评审）的方向输入，对既有每日打卡数据模型（001 已实现 count/target）的取舍在 T014 定夺。**
  9. **空态折叠**：空态下胶囊整行隐藏（`[hidden]`），邀请卡完整落在首屏内（实测邀请卡底缘 736 < 折线 764）。
- 验证：六画板首屏折叠全部实测通过（card1 完整 + card2 在折线下 / 空态邀请卡完整）；交互链 记录→＋转对勾→圆环推进→4/4 成就覆盖层→dismissible；flutter analyze design_tokens.dart 无告警。

### 今日屏 · screen-today.html · R1 · 2026-08-19

- **今日屏 · 六画板 · R1 · 2026-08-19**
- 用户意见原话：「1.'早上好今天也来照顾自己'这地方的字体大小、颜色都不对，其他地方你再review一下 2.四态你再自己审核一下，每个态都有新建第一个目标、并且文字边距也不对，今日进度这个标题也没有了 3.最下面的第 231 天 · 还剩 134 天，这个就不要了。 4.最底部导航栏，根原有图片不一样，我发现figma也没做好，你再参考原有图片复刻一下。 5.保证第一屏正好显示1个卡片，往下滑动再显示其他的。 6.今日进度的卡片，里面文字太多了，你再看看原始的图片是怎么做的。」
- 结论：**修改后再审（6 项全改，R2 待评审）**
- 修订说明（R2 版）：
  1. **问候区回归定稿解剖**：双行结构——`早上好` 16px/600/onSurface + `星行` 14px/400/onSurfaceVariant（此前自创「今天也来照顾自己」单行粗体，字号颜色均偏离）；头像 44px 渐变圆 + 白色单字、右侧改为纯图标黑圆「＋」（36px，去掉文字）与白底铃铛（带未读红点）；全屏做了一轮对照 direction-g 定稿值的自查。
  2. **四态串态缺陷根因修复**：`.st-empty/.st-busy` 的 `display:flex` 覆盖了 UA 对 `[hidden]` 的隐藏——空态邀请卡与忙碌横幅在全部六画板漏出。加全局 `[hidden]{display:none!important}` 兜底；空态现仅在无目标画板出现、忙碌横幅仅在忙碌画板出现（Playwright 逐画板实测）。**「今日进度」标题恢复**：hero 卡顶部 14px/600 次级色（direction-g 定稿值，此前误删）。
  3. **年度卡注释删除**：「第 231 天 · 还剩 134 天」整行移除，年度卡只剩「年度守护」标题 + 时间带 + 图例（达成日/未达成/今天）。
  4. **底部导航按原图复刻**：白色悬浮圆角条（surface + radius-xl + shadow-mid，左右 inset 16/下 16），四页签「今日/目标/周回顾/我的」带图标+文字；**选中项 = 黑色胶囊 chip（白图标白字，横排）**，未选中 = 灰色图标+文字竖排。
  5. **首屏折叠实测调优**：hero 加标题行、圆环 110→130、卡内边距 20/节奏 12——在 390×844 下逐画板量测，card1 底缘 ≤ 滚动视口折线（749.2 ≤ 755.7）、card2 顶缘在折线下方（761.2 > 755.7），四态全部满足；忙碌态另将 display 收为单行「今天很忙，也没关系」+ 横幅上边距 20→12，找回横幅占位（card1 底缘 751 ≤ 折线）。
  6. **进度卡文字瘦身**：删除「今日进度」卡内的鼓励语句与分隔线，回到原图密度——圆环中心 `38%`（18px/700 tnum）+「完成率」小注，右侧仅三行图标统计（图标 14 + 数字 600 tnum + 标签），无多余文案。

---

### 方向对比 · 第 3 轮（2026-08-19）

- **风格方向 · G v3 · R3-3 定稿 · 2026-08-19**
- 用户意见原话：「行差不多了，后面再微调吧，可以继续了」
- 结论：**通过——「柔彩仪表盘」定为 Target 设计方向（FR-009 定稿）**；遗留微调项后续各屏深化时一并处理
- 修订说明：无需修订；进入 T005 令牌收敛。定稿依据 = direction-g.html v3：玻璃壳景深（渐变底幕 + white/60 blur 壳 + white/80 卡）、36px/800 两行 display、110px 青柠 `#b5e550` 粗描边圆环 + 竖排图标统计、计数状态胶囊、双层级按钮（黑实心主操作 / 白描边次操作）、8pt 间距节奏、Inter（可变字体，data URI 内嵌）+ PingFang SC 中文栈。

- **今日屏 · 方向 G v2 · R3-2 · 2026-08-19**
- 用户意见原话：「这个感觉就对了，但是再扣下细节：1.切换忙碌模式、重置是不可以整合到右上角通知的按钮中 2.卡片里面文字的边距看起来有些紧凑 3.字体的效果是默认的吗，感觉没有figma中的效果好。你可以打开figma的设计，里面应该是html，然后截图自己比较看看。」
- 结论：**方向基本通过（G v2 结构获认可），3 处细节修订后再审**
- 修订说明（G v3）：① 移除底部演示控制条，忙碌模式/重置收纳进右上角铃铛的弹出菜单（白玻璃浮层 + 状态点亮指示，点外部关闭）；② 卡片内边距 16→18px、行距 12→14px、标题行高 1.3→1.45、副行行高 1→1.4，主卡内边距对齐参照 20px、圆环与统计列间距对齐参照 gap-6=24px；③ 本地运行用户提供的 Figma 复现工程实测对比（Vite dev server），确认差距源于字体——参照经 Google Fonts 加载 **Inter**；将 Inter 可变字体 latin 子集（48KB woff2）以 data URI 内嵌（保持原型自包含零外链），display 精确对齐 `36px/45px/800/-0.025em`。比对工具的误报（圆环色值/粗细/端点、字体未加载等）经计算样式实测逐条排除。

- **今日屏 · 方向 G v1 · R3-1 · 2026-08-19**
- 用户意见原话：「不只是这个配色和圆角，还要更深层次的调整结构、骨架、布局等等，整个设计层面再提高一个层次。我这有figma复现的代码……你可以分析下排版，然后再继续。」
- 结论：**修改后再审（轮内迭代 v1→v2）**
- 修订说明：分析用户提供的 Figma 复现代码（Vite/React/Tailwind，`三屏UI设计复现.zip`）后按其结构骨架重写：① 页面渐变降为**底幕**，整个 App 为浮在其上的玻璃壳（`white/60 + backdrop-blur`），卡片再叠 `white/80`——两层玻璃景深；② **36px/800 两行编辑级 display 标题**（把今天⏎照顾好）；③ 进度主卡=110px 圆环、10px 粗描边、青柠 `#b5e550`、圆头 SVG dasharray + 右侧**竖排图标统计行**；④ 分类 chips 替换为**带计数的状态胶囊**（数字在前：`5 待打卡` 青柠选中态/`N 已完成`，随打卡实时联动）；⑤ 目标卡按参照解剖重排：色点+分类+右上详情箭头 → 粗标题 → 时间+计数+进度点+36px 黑圆打卡钮（完成→青柠对勾）；⑥ **双层级按钮系统**：主操作黑实心（新建/打卡）、次操作白底描边（铃铛/详情）；⑦ 底导航半透明+blur+发丝线；⑧ 画板改 390×844+灵动岛，间距 24/20/16/12/8 的 8pt 节奏；⑨ 深色板同语言适配（暗紫底幕+深玻璃壳+反色强调）。年度时间带保留为 `white/80` 卡。

### 方向对比 · 第 2 轮（2026-08-19）

- **今日屏 · 方向 D/E/F · R2 · 2026-08-19**
- 用户意见原话：「还是不满意，你看下这个你能复刻吗，我就想要这种的」+ 提供参照图（任务管理 App 三屏设计稿：粉紫渐变底 / 白圆角卡 / 深色进度主卡+绿圆环 / 软彩优先级标签 / 黑色圆形操作钮 / 底部四标签导航）
- 结论：**全部否决；升级为参照物对齐（口头对齐条款）**
- 修订说明：第 3 轮按参照图精确复刻视觉语言（direction-g.html「柔彩仪表盘」）：页面底 `linear-gradient(135deg,#f8e8f0,#f0e8f8)`、白卡 12–16px 圆角 + 轻阴影、深色 #1a1a1a 进度主卡（conic 圆环 + 总数/已完成/待打卡三列统计）、软彩标签（#8fd18f/#f0b27a/#f8a5a5 系）、黑色圆形操作钮、头像问候顶栏（+ / 铃铛）、四标签底部导航；内容映射为 Target 语义（目标卡打卡、今日进度环、保留用户认可的年度时间带为白卡）；深色画板为同语言适配（暗紫黑渐变底、反色强调）。

### 方向对比 · 第 1 轮（2026-08-18）

- **今日屏 · 方向 A/B/C · R1 · 2026-08-18**
- 用户意见原话：「都不是需要的，在设计方面都比较普通，不和我的胃口。他们每个都很普通，看着都是简单的设计，没有深度，样式也不喜欢。我想要苹果的那种现代设计风格，"风格方向 B"里面最上面的倒计时的想法还不错，不过换成一年的时间是不更合适？」
- 结论：**全部否决（修改后再审，轮次 +1）**
- 修订说明：第 2 轮转向 Apple 现代设计语言（SF 系统排版、材质与层次、精确细节、弹簧动效），三个家族内变体：D「系统 Today」（iOS 设置/健康式克制精致）、E「活力数据」（Fitness 式大数字与渐变活力）、F「玻璃层次」（visionOS 式通透景深）。保留 B 的顶部时间带概念并将尺度改为**一年**：365 天刻度带（今日标记 + 按完成情况着色 + 「今年还剩 N 天」），作为三方向共享元素深化。
