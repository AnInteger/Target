# 006 验证指南（quickstart）

> 环境：`flutter analyze && flutter test`；日常验证 `flutter run -d chrome`。

## 阶段 A · 原型评审（当前）

1. 本地服务：`python3 -m http.server 8390 -d design` →
   打开 `http://127.0.0.1:8390/prototypes/index.html`（v3 区 8 屏）
2. 逐屏过审：浅色 → 深色（画板自带）→ 空态/菜单/sheet 画板
3. 六项待裁点（见 reviews.md 006 R1 条目）逐项给出裁定
4. 令牌总览页检查 v3 色板 / 字阶 / 玻璃档

## 阶段 B · 数据面（Phase 1 后）

- [ ] 空库首启无迁移报错（清库重建）
- [ ] 新建目标（只填名称）→ 落库 status=active / pinned=false
- [ ] 富记录：标题必填校验；时长/正文/里程碑可选；补记日 isBackfill=true
- [ ] 里程碑达成：doneAt + kind=milestoneAchievement 记录同事务生成；撤销同步撤除
- [ ] 备份 v7 导出 → 清库 → 恢复（含冲突确认覆盖）→ 全字段往返一致
- [ ] 旧格式（version≤6）导入被明确拒绝
- [ ] 周投入统计手工核算：30+60+45+0+75+90+30=330min=5h30m；无时长记录只计条数

## 阶段 C · 动线（Phase 3 后）

- [ ] C-1 首启空态：无引导 → 空目标页 → CTA 一次进编辑器 → 保存即见置顶卡
- [ ] C-2 记录闭环：底栏记录钮 → 选目标 → 标题 → ✓ → toast 撤销可用 → 目标卡「最近记录」即时刷新
- [ ] C-3 里程碑：里程碑页添加（含描述）→ ⋯ 标记达成附一句 → 详情橙色节点 + 动态 feed「达成：…」+ 汇总 +1
- [ ] C-4 日历：点「日历」直达（无筛选条件）；月导航覆盖所有日期；点选日摘要正确；环填充=当日/月峰值
- [ ] C-5 生命周期：⋯ 暂停 → 默认视图隐藏/筛选可见/记录切换器不列 → 恢复回归；归档同口径；删除二次确认
- [ ] C-6 置顶：菜单置顶/取消；「编辑置顶」模式（− 移出 / ≡ 拖拽 / 图钉加入 / ✓ 完成）生效；目标卡长按弹菜单
- [ ] C-7 编辑器：只填名称可存；日期/节奏/提醒/里程碑任意组合；编辑回填原值
- [ ] C-8 设置：外观三档即时生效且落库；提醒总开关关闭全量取消；备份导出/恢复
- [ ] C-9 深链：target://goals|activity|goal/{id}|record 落对页面
- [ ] C-10 深色：三档主题全屏一致（令牌链路 identical 断言沿用 004 口径）

## 阶段 D · 收口门禁

- [ ] `flutter analyze` 0 issue
- [ ] `flutter test` 全绿（套件重组后）
- [ ] 对比度矩阵正文级 0 FAIL（005 口径）
- [ ] web 构建 + Playwright 走查全动线
- [ ] 删除清单零残留（grep 审计表 3.3 各项）
- [ ] 真机侧载清单（Codemagic ios-unsigned → iLoader，沿用 003/004 回路）
